# GOALS ARCHIVE — finished ticket blocks, verbatim, newest last

- [x] **T1 — App scaffold + toolchain**
  Goal: Vite + React 18 + TypeScript scaffold that builds and tests clean.
  Files: package.json, vite.config.ts, tsconfig*.json, index.html, src/main.tsx, src/App.tsx, src/test/smoke.test.tsx, scripts/gate-heldout.mjs (stub), scripts/fetch-data.mjs (stub).
  Do FIRST — API probe from this sandbox, result recorded in PROGRESS.md:
  `curl -s -o /dev/null -w "%{http_code}" "https://api.deadlock-api.com/v1/analytics/item-stats?hero_id=1"` and `curl -s -o /dev/null -w "%{http_code}" "https://assets.deadlock-api.com/v2/heroes"`. If either is unreachable, say so LOUDLY in PROGRESS.md — T2 depends on it.
  Acceptance: `npm install` clean on Node ≥20; `npm run build` succeeds; `npm test` runs a jsdom smoke test mounting `<App/>` (vitest + jsdom + @testing-library/react); `npm run gate:heldout` exists (trivially passes while src/generator is empty); `npm run fetch-data` exists as stub exiting 1 with "not implemented".
  Verify: `npm run build && npm test && npm run gate:heldout`

- [x] **T2a — Data pipeline script (`npm run fetch-data`) — code only, no network**
  Goal: fully implement scripts/fetch-data.mjs per the Outputs list below, plus src/test/snapshots.test.ts. The sandbox cannot reach the APIs (see SANDBOX EGRESS above) — do NOT run the real fetch; the orchestrator runs it locally in T2b.
  Files: scripts/fetch-data.mjs, src/test/snapshots.test.ts.
  Requirements: rerunnable; sequential requests with ≥300 ms sleep; one retry after 5 s on HTTP 429; prune fields per the Outputs list; write `public/data/meta.json` last (fetched_at + counts). snapshots.test.ts asserts the counts/required fields below against committed snapshots, but SKIPS cleanly (vitest skipIf) when `public/data/meta.json` is absent.
  Acceptance: `node --check scripts/fetch-data.mjs` clean; gates green with snapshots absent (tests skip, not fail). Real-data verification happens in T2b.
  Verify: `node --check scripts/fetch-data.mjs && npm run build && npm test && npm run gate:heldout`
  Outputs:
  - `public/data/items.json` — all upgrade items from `https://assets.deadlock-api.com/v2/items/by-type/upgrade`: id, class_name, name, cost, item_tier, item_slot_type, image webp URL, and the properties/tooltip fields needed for the detail card (stat lines + active/passive descriptions). ≥200 shopable items.
  - `public/data/heroes.json` — ACTIVE heroes only (exclude disabled/in-development): id, name, card image URL, base stats + per-level stat growth, and the hero's 4 ability ids + real names.
  - `public/data/analytics/hero-<id>.json` — for EVERY active hero: item-stats and ability-order-stats from `https://api.deadlock-api.com/v1/analytics/...?hero_id=N`, pruned to scoring fields (item_id, wins, matches/usage; ability sequences with wins/matches).
  - `public/data/analytics/infernus-permutations.json` — item-permutation-stats for hero_id 1 only (budget decision; other heroes go without).
  - `public/data/personal/matches.json` — account 267836488 match history, standard matchmaking modes only, pruned to hero_id, won/lost, duration seconds, start_time.
  - `public/data/zergggy/matches.json` — VALIDATION ONLY. Account 35187362: Infernus (hero_id 1) real matchmaking matches (exclude private lobby / bot modes), newest ~30; fetch each match's `/v1/matches/{id}/metadata` and prune to match_id, won, and Zergggy's item purchases as [{item_id, game_time_s}]. ≥20 matches with purchase data required.
  - `public/data/meta.json` — fetched_at + counts.
  Notes (judgment calls, unverified from this sandbox): exact upstream JSON field names for items/heroes/analytics/match endpoints are not confirmed here (no egress) — pruning uses defensive multi-key fallback (`firstDefined`) and is expected to need a spot-check/adjustment pass against real payloads when T2b runs the fetch for real.

- [x] **T2b — Run the fetch + commit snapshots — ORCHESTRATOR ONLY, fires must SKIP this ticket** (it needs API access the sandbox doesn't have; skip silently, no PROGRESS note needed, and take the next unchecked ticket)
  Goal: orchestrator runs `node scripts/fetch-data.mjs` on the local machine, verifies, commits `public/data/**`.
  Acceptance: snapshots.test.ts passes with data present; `du -sh public/data` < 15 MB; snapshots committed and pushed.
  Verify: `node scripts/fetch-data.mjs && npm test`

- [x] **T3 — Deterministic build generator** (needs T2b's committed snapshots for its tests; if `public/data/meta.json` is absent, skip to T4 — its tests are fixture-based)
  Goal: src/generator/ produces ≥2 named builds for ANY active hero from aggregate snapshots only.
  Files: src/generator/**, src/test/generator.test.ts, scripts/gate-heldout.mjs (real implementation).
  Spec: a documented, deterministic scoring function (stable tie-breaks by item id). Inputs: items.json, heroes.json, analytics/hero-<id>.json, infernus-permutations.json when present — NEVER zergggy data. Score items on: win rate with confidence damping by sample size (shrink toward the hero mean), usage rate, stat value per soul, tier/soul thresholds by game phase (early/mid/late budgets), per-build archetype weight profiles derived from the hero's scaling stats in heroes.json (Infernus: gun/fire-rate archetype vs spirit/afterburn archetype), and an active/passive balance cap. Output per build: name, ordered buy list of ≥12 items grouped early/mid/late with per-item cost + running soul total, plus ability level-up sequence (unlock order + tier-upgrade order) for the hero's 4 real abilities driven by ability-order-stats (deterministic sensible fallback when a hero lacks data).
  Acceptance: generator.test.ts asserts — two runs on the same snapshot are deep-equal; Infernus + ≥3 other heroes each yield ≥2 builds, ≥12 items, nondecreasing running totals, 4 named abilities; gate:heldout greps src/generator for "zergggy" (case-insensitive) and fails on any hit.
  Verify: `npm test && npm run gate:heldout`
  DATA-GAP JUDGMENT CALLS (see PROGRESS.md 2026-09-01 entry for detail): every hero's `heroes.json.base_stats` came back structurally identical (a T2 field-mapping gap — `starting_stats`/`level_scaling` don't carry real per-hero values from this API), so archetype weighting uses item `item_slot_type` (weapon/spirit) + a `vitality` mid-bonus instead of hero scaling stats. Every `analytics/hero-<id>.json` row's `ability_order_stats[].sequence` is null (same kind of gap), so the ability level-up sequence always uses the spec's documented fallback (unlock 1-4, then upgrade round-robin ×2) rather than real per-hero order. `active_description`/`passive_description` are null for all 251 items, so the active/passive split for the balance cap uses a stat-line heuristic (nonzero `AbilityCooldown`) instead. None of this touches src/generator's zergggy-free guarantee; all three are flagged for an orchestrator follow-up on scripts/fetch-data.mjs field names if a future ticket wants the real data.

- [x] **T4 — Held-out validation + personalization**
  Goal: src/validation/ scores generated builds against Zergggy's core set; src/personalization/ computes one displayed insight.
  Files: src/validation/**, src/personalization/**, src/test/validation.test.ts.
  Spec: core set = items appearing in ≥30% of his sampled matches, win-weighted (each match weight: win 1.5, loss 1.0 — document exact formula in README); items below threshold are "experiments", excluded entirely. Per generated build: per-item core/not-core flag + agreement % in [0,100] = weighted blend of core-set overlap and buy-order agreement (pairwise order concordance on shared items) — document the formula. Personalization: median standard-match duration for account 267836488 → an annotation shaping/labeling the late-game budget line. Frame all validation output as "how well the generator did", never as a build source.
  Acceptance: validation.test.ts covers core-set math on a small fixture, agreement bounds, and experiments exclusion; only src/validation/ reads zergggy paths.
  Verify: `npm test && npm run gate:heldout`
  Formulas (README still owes the polished writeup — T7): `coreSetShare(item) = (Σ matchWeight(m) for matches m containing item) / (Σ matchWeight(m) over all sampled matches)`, matchWeight = 1.5 if won else 1.0, threshold 0.30 inclusive. `agreement% = round(100 * (0.6 * coreSetOverlap + 0.4 * buyOrderAgreement))`, clamped [0,100], where coreSetOverlap = |build items in core set| / |core set|, and buyOrderAgreement is the win-weighted pairwise concordance (per src/validation/orderPreferences.ts) of the build's shared core-set items against Zergggy's majority buy order, defaulting to 0.5 (neutral) for a pair/build with no signal.
  JUDGMENT CALL: "only src/validation/ reads zergggy paths" is scoped to production modules — src/test/ is exempt since snapshots.test.ts (T2a) already asserts the zergggy snapshot's own shape, which is data-integrity testing, not generator/scoring logic; see src/test/validation.test.ts's held-out isolation test for the exact boundary.

- [x] **T5 — Mobile-first interactive UI**
  Goal: the full app per spec, designed at 390×844.
  Files: src/App.tsx, src/components/**, styles.
  Spec: hero picker (all active heroes, Infernus default); per build: name, buy list grouped early/mid/late with cost + running soul total, item rows with shop image + Zergggy core/not-core badge and per-build agreement % (validation shown for Infernus; other heroes render builds without the validation report); ability level-up sequence with real ability names; tapping any item opens a detail card (image, cost, tier, slot type, stat lines, active/passive text from snapshot tooltip data); personalization insight line. No horizontal scroll at 390px; tap targets ≥40 px; desktop = centered column (~max-width 480px).
  Acceptance: jsdom tests boot the real `<App/>`: Infernus default with ≥2 builds of ≥12 items; tapping an item shows its detail card with cost/tier/slot; selecting 3 other heroes renders builds + ability orders without errors; badges and agreement % present for Infernus.
  Verify: `npm run build && npm test`
  Implementation notes: src/data/loaders.ts fetches items/heroes/analytics/personal matches from `/data/**` (static files in prod, fs-backed fetch mock in src/test/setup.ts for jsdom); src/validation/loadMatches.ts + a new `validateBuildsAgainstHeldOut(builds)` in src/validation/index.ts keep the zergggy fetch inside src/validation/ so App.tsx never references it (isolation test still passes). Held-out validation runs only when the selected hero is Infernus (`src/constants.ts` INFERNUS_HERO_ID=1); other heroes render `validation={null}` and get no badges/agreement chip.
  DATA-GAP JUDGMENT CALL: some `stat_lines[].value` entries in items.json are display-metadata objects (`{label, prefix, postfix, ...}`) instead of numeric strings — a T2 extraction artifact, out of scope to fix here. Widened `StatLine.value` to `unknown` (generator/types.ts; statUtils.ts's `Number(value)` already coerced non-numeric input to 0, unaffected) and added a defensive `formatStatValue` in ItemDetailSheet that falls back to the object's `label` field, or "—", instead of crashing React's child renderer.
  Verified: `npm run build` clean; `npm test` 30 passed/2 skipped (new src/test/app.test.tsx: Infernus default/badges/agreement, item-detail-card open+close, 3 non-Infernus heroes render without validation); `npm run gate:heldout` OK; manual grep confirms no "zergggy" string outside src/validation/ and src/test/. NOT verified: real-browser layout/tap-targets at 390×844 (jsdom has no real layout engine) — left for T6.

- [x] **T6 — Real-browser verification pass**
  Goal: verify what jsdom structurally can't see.
  Files: e2e/** (Playwright) or documented audit in PROGRESS.md.
  Spec: try installing Playwright + chromium in this sandbox. If it installs: at 390×844 assert no horizontal overflow (scrollWidth ≤ 390) on the main screen, an open detail card, and a non-Infernus hero; spot-check tap-target boxes ≥40 px. If browsers can't install, do a targeted CSS audit (fixed widths, overflow-x) and record "unverified in real browser" honestly. Also: production `npm run build` + preview smoke — app must not crash with network disabled (item images may 404; that's acceptable offline).
  Verify: `npx playwright test` if installed, else the documented audit; `npm run build`
  Outcome: Playwright DID install and run — this cloud sandbox ships a pre-installed Chromium at `/opt/pw-browsers/chromium` (per the environment's own operating notes). Installed `@playwright/test` as a devDependency (`PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm install -D @playwright/test`, no browser re-download); `playwright.config.ts` points `launchOptions.executablePath` at the pre-installed binary and runs against `npm run preview` (production build) at the 390×844 design viewport. `npm run test:e2e` added as the run command.
  e2e/mobile.spec.ts (5 tests, all passing against the real production build): (1) Infernus default screen — 2 build cards render, `document.documentElement.scrollWidth <= clientWidth` (no horizontal overflow); (2) same overflow check with an item detail card open; (3) same overflow check after switching to a non-Infernus hero; (4) tap-target spot check — hero `<select>`, an item row button, and the detail sheet's close button all measure ≥40px in the constrained dimension; (5) offline resilience — routes to the item-image host (`assets-bucket.deadlock-api.com`) aborted, app still boots and opens a detail card with zero `pageerror` events (broken `<img>` 404s are the only visible effect, as expected).
  BUG FOUND AND FIXED DURING THIS TICKET (not by the e2e suite — by `npm test` immediately after adding the Playwright files): vitest's default test glob (`**/*.{test,spec}.*`) was also picking up `e2e/mobile.spec.ts` and failing on Playwright's `test.describe` (different test-runner globals) — added `test.exclude: ['**/node_modules/**', 'e2e/**']` to vite.config.ts's vitest block to scope the two runners to their own directories.
  Verified: `npm run build` clean; `npx playwright test` 5/5 passed against the real Chromium + production preview server; `npm test` 30 passed/2 skipped (still green after the vitest exclude fix); `npm run gate:heldout` OK. `test-results/`/`playwright-report/` (Playwright's local run artifacts) added to `.gitignore`, not committed.

- [x] **T7 — README + acceptance sweep**
  Goal: README.md documenting everything; final walk of the acceptance checklist in ROADMAP.md.
  Files: README.md.
  Spec: run instructions (`npm install`, `npm run fetch-data`, `npm run dev`); scoring-function inputs and exact weights + determinism statement; the ≥30% win-weighted core-set rule with experiments excluded; held-out design (generator never reads Zergggy — grep gate); every judgment call so far (pull from PROGRESS.md Decisions); parked/unverified items.
  Acceptance: README covers all of the above; all gates green; every box in ROADMAP.md's acceptance checklist verified or explicitly listed as failed/unverified — fix failures before checking this ticket.
  Verify: `npm run build && npm test && npm run gate:heldout`
  Outcome: wrote README.md covering run instructions, the data pipeline's output table + politeness rules, the generator's exact composite-score formula (0.35 confidence-damped win rate / 0.25 usage rate / 0.25 value-per-soul / 0.15 archetype bias, tie-break on ascending item id, determinism statement), the held-out core-set formula (win-weighted 1.5/1.0, threshold 0.30) and agreement% formula (60% core overlap + 40% buy-order concordance), personalization, UI/mobile-first summary, and a "Known data gaps and judgment calls" section pulled from every prior PROGRESS.md entry (identical base_stats across heroes, null ability_order_stats sequences, null active/passive descriptions, non-numeric stat_lines values, the hand-built scaffold + vitest bump, sandbox egress, Playwright happening to be pre-installed).
  Walked ROADMAP.md's 10-item acceptance checklist and checked every box, with two given an explicit scope note rather than silently checked: (1) item detail cards show `stat_lines` in place of active/passive descriptive text, because that text is null for all 251 items in this snapshot (a T2/T3-documented upstream gap, not a code defect) — nothing to fix, nothing to display; (2) core/not-core badges + agreement % are Infernus-only by design, since Zergggy's held-out sample was only ever collected for Infernus (see T2a's Outputs list) — every other checklist line is a plain, unqualified pass. ROADMAP.md's own checklist boxes were updated to [x] to match, each with a one-line pointer back to README.md for detail.
  Verified: `npm run build` clean, `npm test` 30 passed/2 skipped, `npm run gate:heldout` OK — README/ROADMAP edits don't touch code, re-ran the full gate set anyway per the standing GATES rule.
  T7 archived to GOALS_ARCHIVE.md. GOALS.md's Open tickets queue is now empty — T1 through T7 are all done, ROADMAP.md's acceptance checklist is fully green. Next fire: pull a ticket from ROADMAP.md's "known gaps / stretch" list (per-hero archetype tuning beyond Infernus, situational item swaps, validation against additional top players, PWA image caching, build export format) if continuing to grow the project is wanted — none of that was in scope for T1–T7.

- [x] **T8 — item detail: show only meaningful stats (user request 2026-09-01)**
  - Goal: the item detail sheet stops rendering "a bunch of unneeded 0's" — a stat line renders ONLY if it carries real information: numeric value ≠ 0, or a non-numeric display value with a usable label. Zero-valued lines and "—" placeholders are hidden. If every line is filtered out, hide the Stats section header too.
  - Files: `src/components/ItemDetailSheet.tsx`, `src/test/app.test.tsx` (extend; a small pure helper + unit test is fine if you extract the filter).
  - Acceptance: (1) a real item whose stat_lines include zero values shows only the nonzero ones (find one in the committed snapshot from the test, don't hardcode assumptions); (2) Cost/Tier/Slot rows unaffected; (3) an item with no meaningful lines shows no Stats section; (4) existing detail-sheet tests still pass.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e` (components changed; chromium is preinstalled at `/opt/pw-browsers/chromium`, config already points there).
  Outcome: added `isMeaningfulStatLine(value)` in ItemDetailSheet.tsx — nonzero numbers and unit-suffixed numeric strings (e.g. "7m", parsed via `parseFloat` since `Number("7m")` is NaN) are kept; zero values ("0", "0m", 0) and unparseable junk strings with no label (e.g. a real snapshot value literally `"asdasd"`) are hidden; a non-numeric display-metadata object (`{label, prefix, ...}`, the known upstream gap where the raw numeric value is missing entirely) is kept whenever it has a non-empty `label`, since we can't determine its zero-ness and the label itself is the information. JUDGMENT CALL: a bare non-numeric string with no label/object wrapper (only "asdasd" was found in the real snapshot) is hidden — it fits neither of the ticket's two "meaningful" buckets, so hiding it matches the letter of the spec over showing raw noise.
  Wrapped the stats `<ul>` in a new `.item-detail-sheet__stats-section` div with an `<h3>Stats</h3>` heading (didn't exist before); the whole section renders only when `meaningfulLines.length > 0`, satisfying "hide the Stats section header too."
  Verified against real data: item 1548066885 "Extended Magazine" (found via a throwaway probe, not hardcoded into the test) has 15 stat_lines, 10 of which are zero-valued ability-mechanics boilerplate — filter reduces it to the 5 real ones (AbilityUnitTargetLimit=1, AbilityCooldownBetweenCharge=-1.0, ChannelMoveSpeed=-1, BonusClipSizePercent=30, BaseAttackDamagePercent=8).
  Tests added to src/test/app.test.tsx: 5 pure-function cases for `isMeaningfulStatLine` (zero/nonzero numbers, zero/nonzero unit-suffixed strings, junk string, labeled object, empty-label object) plus 2 snapshot-backed `ItemDetailSheet` render tests (real item with mixed zero/nonzero lines shows only the nonzero ones + Cost/Tier/Slot intact; an item with zero meaningful lines, if one exists in the snapshot, shows no Stats section) — both `describe.skipIf(!hasSnapshots)`, consistent with the rest of the suite.
  Verified: `npm run build` clean; `npx vitest run` 36 passed/2 skipped (was 30/2, +6 new); `npm run gate:heldout` OK (change is UI-only, doesn't touch src/generator); `npx playwright test` 5/5 (existing e2e specs don't assert on stat-line content, so unaffected — confirmed they still pass rather than assuming it).

- [x] **T9 — weight scoring toward high-elo players (user request 2026-09-01)**
  - Goal: the generator's win-rate and usage components prefer high-elo evidence. Data is ALREADY COMMITTED (orchestrator fetched it locally — do NOT attempt API calls): each `public/data/analytics/hero-N.json` now has `high_badge_item_stats` (same shape as `item_stats`: `{item_id, wins, matches}`) filtered to matches with average badge ≥ 81 (Phantom+; `high_badge_min` field + `meta.json.high_badge_min` record the cutoff). Infernus: 138 high-badge items vs 156 overall, ~1.15M item-match rows.
  - Spec: per item, when its high-badge sample is large enough (matches ≥ a documented MIN_SAMPLE constant, suggest 100), compute win-rate/usage from a blend weighted ≥70% toward the high-badge stats (suggest 0.75/0.25); below MIN_SAMPLE, fall back smoothly to overall stats (a sample-proportional blend is fine — document the exact formula in code + README). Keep the existing confidence damping (shrink-to-hero-mean K=50) applied AFTER blending. Determinism and stable tie-breaks unchanged.
  - HELD-OUT REMINDER: if the Zergggy agreement % moves, record the new number in PROGRESS.md as a finding — do NOT tune the blend to raise it.
  - Files: `src/generator/` (types/score/index), `src/data/loaders.ts` + `src/test/setup.ts` only if the analytics type needs the new field threaded, `src/test/generator.test.ts`, README's formula section.
  - Acceptance: (1) fixture test: an item whose high-elo win rate beats its overall win rate outscores the reverse case when samples are adequate; (2) fixture test: item below MIN_SAMPLE high-badge matches degrades toward overall-only scoring; (3) determinism test still green; (4) all 38 heroes still generate ≥2 builds/≥12 items (existing tests); (5) README documents the blend formula + Phantom+ cutoff.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout`.
  Outcome: `HeroAnalytics` gained `high_badge_item_stats`/`high_badge_min` (types.ts). `score.ts`'s `blendHighBadgeStat` computes, per item, `weight = 0.75 * min(highMatches/100, 1)` then blends win rate and usage ratio (each as `weight*high + (1-weight)*overall`); `dampedWinRate` applies the unchanged K=50 shrink-to-hero-mean formula to the blended rate + a correspondingly blended "effective matches" figure (not a real count, just the same weight applied to both sample sizes, used only as the damping-confidence proxy) instead of raw wins/matches. `scoreItem`'s inputs were renamed `overallWins/overallMatches/highWins/highMatches` + `maxOverallMatches/maxHighMatches`; `index.ts` threads a second `high_badge_item_stats` lookup map and `maxHighBadgeItemMatches` through unchanged otherwise. `loaders.ts`/`setup.ts` needed no changes (generic JSON passthrough).
  `generator.test.ts`'s new "scoreItem high-elo blend" describe (pure fixtures, not snapshot-gated) covers acceptance (1) directly — item B (weak overall 10%/strong high-elo 90%, 1000 high-badge matches) outscores item A (the mirror image) — and (2) — a 5-high-badge-match item's blended rate sits within 0.05 of its pure overall rate, and zero high-badge evidence collapses to exactly the overall rate.
  HELD-OUT FINDING (per the standing reminder, not tuned toward): re-ran Infernus's real generated builds against the Zergggy snapshot before and after this change (`validateBuildAgainstMatches`, throwaway test file, deleted after) — agreement % is IDENTICAL both ways: Weapon 35%, Spirit 51%. The blend didn't reorder Infernus's top-scoring items enough to change either build's item selection for this snapshot.
  README's "High-elo weighting (Phantom+)" section documents the exact weight/blend formulas, the ramp behavior, and the before/after agreement finding above.
  Verified: `npm run build` clean; `npx vitest run` 39 passed/2 skipped (was 36/2, +3 new); `npm run gate:heldout` OK (5 files, still zero zergggy references — the blend only reads `high_badge_item_stats`, a field name with no "zergggy" substring, so grep-based isolation holds).

- [x] **T13 — one recommended build per hero (user request 2026-09-01; deliberately ordered before T10 so the restyle lands on the single-build layout)**
  - Goal: the app shows exactly ONE good build per hero, not a list of alternatives. Generator keeps building its archetype candidates internally but exports a single best build: highest total composite score across candidates, stable tie-break (ascending archetype name, then ascending item ids). Determinism preserved.
  - HELD-OUT RULE (critical): the pick must use ONLY generator-internal scores. Never consult `src/validation/` output or Zergggy agreement to choose the build — that would be tuning to the held-out set. The agreement % (Infernus) is computed for the ONE exported build, displayed as before, and recorded in PROGRESS.md as a finding.
  - UI: one build card; keep the winning archetype visible as a subtitle (e.g. "Spirit build") so the choice is legible. Ability order + personalization annotation attach to that single build.
  - Files: `src/generator/index.ts` (+types if the return shape changes), `src/App.tsx`, `src/components/BuildCard.tsx`, `src/test/generator.test.ts` (≥2-builds assertions become exactly-1), `src/test/app.test.tsx`, `e2e/mobile.spec.ts` if it counts builds, README's generator section.
  - Acceptance: (1) every hero exports exactly 1 build with ≥12 items + 4 named abilities; (2) determinism test still green; (3) fixture test: candidate with higher total score wins, tie broken by archetype name; (4) Infernus still shows the agreement chip for the exported build; (5) all suites green.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: `generateBuilds(hero, items, analytics)` now returns a single `Build` (was `Build[]`). Internally `buildForArchetype` still builds both weapon/spirit candidates, now each paired with a `totalScore` (sum of its selected items' composite `scoreItem` scores, using the same per-archetype `scored` list already computed for greedy selection — no extra scoring pass). New exported pure function `pickBestBuild(candidates: ScoredCandidate[])` picks highest `totalScore`, ties broken by ascending archetype name (`'spirit' < 'weapon'`), then by the build's own item-id sequence (lexicographic) as a final deterministic fallback. Uses ONLY these generator-internal scores — never touches `src/validation/` or agreement output (held-out rule respected).
  Build.name changed from `"<Hero> Weapon Build"`/`"<Hero> Spirit Build"` to `"<Hero> Build"`; `BuildCard` now renders the winning archetype as a `.build-card__archetype` subtitle ("Weapon build"/"Spirit build") under the name, satisfying the legibility requirement without duplicating "Build" in the header.
  App.tsx: `builds: Build[]` state renamed to `build: Build | null`; `validation: Map<string, ValidationReport>` renamed to `validation: ValidationReport | null` (still calls the unchanged `validateBuildsAgainstHeldOut`, just wrapped as `[generated]` and unwrapped via `.get(generated.name)` — validation module untouched, no need to special-case a single-build path there). Build list renders one `BuildCard` instead of `.map`.
  Tests updated: generator.test.ts's ">=2 valid builds" test becomes "exactly 1 valid build" (no more looping over `builds`); new `describe('pickBestBuild', ...)` with 2 pure fixture tests (higher total score wins regardless of input order; tie breaks to 'spirit' over 'weapon') — not snapshot-dependent. app.test.tsx: agreement-badge count now `toBe(1)`, `.build-card` count now `toBe(1)` in both the Infernus and 3-other-heroes tests. e2e/mobile.spec.ts: all four `.build-card` count assertions changed from `toHaveCount(2)` to `toHaveCount(1)`; the Infernus test's title updated to say "exactly 1 build". validation.test.ts's real-snapshot agreement test changed `const [build] = generateBuilds(...)` to `const build = generateBuilds(...)` (no longer array-destructurable). README's generator/UI sections rewritten to describe the internal-two-candidates/exported-one-build shape and the pick rule.
  CAUGHT BY THE HELD-OUT GATE ITSELF: an early draft of `pickBestBuild`'s doc comment used the literal word "Zergggy" and `gate:heldout` (correctly) failed the build on it — reworded to "held-out agreement score" with no literal match; left in this note as a demonstration the gate actually catches what it's meant to, not just decoration.
  HELD-OUT FINDING: Infernus's exported build (whichever archetype wins the internal score comparison) still shows an agreement chip; the pick logic never reads validation output so this is unaffected by design, not a claim to re-verify per-run.
  Verified: `npm run build` clean; `npx vitest run` 41 passed/2 skipped (was 39/2, +2 new pickBestBuild fixtures); `npm run gate:heldout` OK (5 files, zero held-out references); `npx playwright test` 5/5 against the real production build. Note: `node_modules` was absent at the start of this run (fresh container) — `npm ci` was run first; not itself part of T13's diff.

- [x] **T14 — item stats render like the real in-game tooltip (user bug report 2026-09-01: "ghost stats" like AbilityUnitTargetLimit shown)**
  - Why: `stat_lines` is the item's FULL engine property bag — most keys are internal and never displayed in-game. The game's tooltip is defined by the item's tooltip sections, and the orchestrator has now committed exactly that as a new `stat_sections` field on every item in `public/data/items.json` (refetched 2026-09-01; 237/251 items have ≥1 section, the rest have `[]`).
  - Data shape (per item): `stat_sections: [{ type: 'innate'|'active'|'passive'|null, description: string|null (plain-text ability description), stats: [{ key, label, value: string|null, prefix, postfix, elevated: boolean }] }]`. Also new: `is_active_item: boolean` (the game's real flag — prefer it over the cooldown heuristic wherever actives are detected for display).
  - Goal: ItemDetailSheet renders `stat_sections` INSTEAD of `stat_lines` (stat_lines remains scoring-only — do not delete it from types/loaders). Per section: optional type heading (Active/Passive; innate = no heading, it's the top block in-game), description paragraph if present, then stat rows: `label  <prefix><value><postfix>`. Prefix `{s:sign}` is a template token meaning "show the value's sign" → render `+` for positive values (`-` comes from the value itself); literal `+`/`-` prefixes render as-is. Postfixes are plain units (`%`, `s`, `m`, `HP/s`, …) — render verbatim, trimmed. Hide stats whose `value` is null (25 exist). Elevated stats (`elevated: true`) are the game's emphasized big stats — render them visually stronger (bolder/larger), first in their section (data is already ordered elevated-first).
  - T8's zero-value filter logic applies to these rows too (a "0"-valued stat is still noise); its pure helper can be reused. Items with empty `stat_sections` show no stats block at all.
  - Files: `src/components/ItemDetailSheet.tsx`, `src/generator/types.ts` or wherever the Item type lives (add the two fields), `src/test/app.test.tsx`.
  - Acceptance: (1) Extended Magazine's sheet shows exactly "Max Ammo +30%" and "Weapon Damage +8%" (elevated Max Ammo emphasized) and NO engine keys; (2) an active item (e.g. Alchemical Fire) shows its Active section with description + stats; (3) ghost keys like AbilityUnitTargetLimit/ChannelMoveSpeed never render for items whose sections don't list them; (4) null-value stats hidden; (5) all suites green.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: `Item` type (generator/types.ts) gained `stat_sections: StatSection[]` and `is_active_item: boolean`, plus new `StatSection`/`StatSectionStat` interfaces (re-exported from `src/generator`'s barrel). `ItemDetailSheet` now renders `stat_sections` exclusively — `stat_lines` stays untouched in types/loaders/generator (scoring-only, per the ticket). Per section: heading only for `active`/`passive` (`SECTION_HEADINGS` lookup; `innate`/`null` get none), description paragraph if present, then stat rows via new `formatSectionStatValue(stat)`. Reused T8's `isMeaningfulStatLine` directly on each stat's `value` — it already returns `false` for `null` (falls through every branch), so the null-hiding requirement needed no new code.
  `formatSectionStatValue`: `{s:sign}` prefix renders `+` only for a positive parsed value (a negative value's own `-` is already in the string, so nothing is added); literal `+`/`-` prefixes pass through as-is (verified real values are unsigned in those cases — spot-checked 8 real `+`/`-` prefixed stats, all unsigned). JUDGMENT CALL (documented in README + code comment): 125/1093 real stat_sections values already have their unit baked into the value string (e.g. `value: "10m"`, `postfix: "m"`, an upstream fetch-data.mjs extraction quirk, not something this ticket touches at the source) — postfix is skipped when the trimmed postfix already terminates the value string, to avoid rendering "10mm".
  Removed the now-dead `item.active_description`/`item.passive_description` renders (both fields are still `null` for all 251 items — unchanged upstream gap — and `stat_sections`' own per-section `description` is the real replacement source); also deleted the now-unused `formatStatValue` helper (was stat_lines-only display formatting, no longer called from anywhere).
  Rewrote app.test.tsx's `ItemDetailSheet (real snapshot)` describe block against real data (no hardcoded item lists beyond the two named in the ticket): Extended Magazine renders exactly "Max Ammo+30%"/"Weapon Damage+8%" with Max Ammo carrying the elevated CSS class and zero ghost keys (`AbilityUnitTargetLimit`/`ChannelMoveSpeed` absent); the first real item with an `active` section shows an "Active" heading + its description when present; the first real item with a `value: null` stat hides that stat's label; an item with empty `stat_sections` (if one exists) renders no `.item-detail-sheet__stats-section` at all.
  Manual real-browser check (temp Playwright spec, not committed): opened a real item's detail sheet against the built app — "Spirit Power +18" rendered bold/larger (elevated) above "Out of Combat Regen +1.5" (normal weight), no ghost keys, no "Stats" header text (removed, replaced by per-section headings) — screenshot confirmed layout before deleting the temp spec.
  README updated: new "Item detail tooltip (`stat_sections`, T14)" subsection under UI; the stale "active_description/passive_description ⇒ shows stat_lines" and "stat_lines[].value can be a display-metadata object ⇒ detail card falls back to label" gap notes rewritten to match the new stat_sections-only rendering; ROADMAP acceptance-checklist bullets in README updated for T13's single-build shape while touching that section anyway.
  Verified: `npm run build` clean; `npx vitest run` 43 passed/2 skipped (was 41/2, net +2 after rewriting 2 T8 tests into 4 T14 tests); `npm run gate:heldout` OK (5 files, zero held-out references — stat_sections/is_active_item are display-only fields the generator never reads); `npx playwright test` 5/5 (existing e2e specs don't assert on stat-tooltip content, confirmed still green; a temporary ad-hoc spec did the visual spot-check above and was deleted, not committed).

- [x] **T10 — restyle build display to match Deadlock's in-game build browser (user request 2026-09-01, from a reference screenshot)**
  - Goal: builds render like the in-game build editor. The reference (user-provided screenshot of the real game; fires can't see it — this spec is authoritative):
    - Each game phase is a titled horizontal section panel — "Early Game", "Mid to Late Game" — on a parchment/tan panel (`#b8a98c`-ish, subtle texture ok) over the app's dark background. Map our early→"Early Game", mid+late→"Mid to Late Game" or keep three sections with in-game-style titles — your call, document it. A third lighter-blue-tinted section titled "Testing" with an "OPTIONAL" chip is how the game shows experimental items — if we have leftover/near-miss items available cheaply, use it; otherwise omit (do NOT invent data for it).
    - Item cards: square art tile on top, item name on a small light label strip below. Tile background color by slot category: weapon = amber/orange (~`#d18b21`), vitality = green (~`#7ab82f`), spirit = purple (~`#8c5fc7`). Item image centered on the tile (images come from snapshot URLs; broken/blocked images must degrade to the colored tile + name, never a broken-image icon).
    - Tier badge: a colored corner ribbon folded over the tile's top-right with the tier as a roman numeral (I–IV). Active items get a small "ACTIVE" chip like the game's imbue/active tags.
    - Header stays app-styled (hero picker etc.) — this ticket is the build card grid + section panels, not a full chrome clone (T12 does app-wide cohesion). Do not copy game logos/fonts.
    - DESIGN.md is AUTHORITATIVE for all colors, type, motion, and quality floor — read it first; it supersedes the approximate hexes above. Typography lands in T12; until then use DESIGN.md's fallback stacks.
  - Mobile-first still rules: at 390×844 sections stack vertically, cards wrap in a grid (~4 per row), tap targets stay ≥40px, no horizontal page scroll. Keep the existing tap-to-open detail sheet, core/experiment badges, and agreement chip working (restyle, don't remove).
  - Files: `src/components/BuildCard.tsx`, `src/components/ItemRow.tsx` (likely becomes an item card), `src/styles.css`, `src/App.tsx` if section grouping moves, `src/test/app.test.tsx`, `e2e/mobile.spec.ts` (update selectors/assertions for the new card layout).
  - Acceptance: (1) phases render as titled section panels with wrapped item-card grids; (2) card shows colored tile by slot type + name label + roman-numeral tier corner; (3) `npm test` app suite green (update assertions to the new DOM); (4) e2e 5/5 at 390×844 incl. no-overflow + ≥40px tap targets + blocked-image-host boot.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: Added the full DESIGN.md `:root` token block to styles.css (parchment/abyss/navy palette, slot colors, khaki/violet accents, plus two convenience font-stack tokens already matching DESIGN.md's documented fallbacks so T12's `@fontsource` swap is additive-only) and pointed `body`'s background/color/font-family at it. JUDGMENT CALL: kept two display panels ("Early Game" / "Mid to Late Game") rather than three, since DESIGN.md's own layout-concept section explicitly specifies that pairing — treated as more authoritative than the ticket text's "your call" phrasing; documented in a BuildCard.tsx comment. The "Testing"/optional third panel is omitted entirely: the generator exposes no leftover/near-miss item data structure to populate it with, and inventing one would violate the standing "never fabricate data" rule — also documented in-code.
  `BuildCard.tsx`: replaced the old `PHASES`/`PHASE_LABELS` 3-way loop with a `DISPLAY_SECTIONS` array (`{key, label, phases}[]`) collapsing `mid`+`late` into one section; each section renders inside new `.phase-panel` > `.phase-panel__title` + `.phase-panel__strip` > `.item-list` markup. The ability-order block at the bottom is untouched (T11's scope).
  `ItemRow.tsx` fully rewritten into a vertical item-card: `.item-card__tile.item-card__tile--{weapon|vitality|spirit}` (slot-colored square) containing an optional `.item-card__active-chip` ("Active", shown when `item.is_active_item` — the real T14 flag, preferred over the generator-internal `isActiveItem()` cooldown heuristic in statUtils.ts which stays scoring-only), a `.item-card__tier` corner ribbon via new `tierLabel(tier)` (roman I–IV; falls back to the plain number for item_tier 5 rather than inventing "V" — confirmed via a real-snapshot query that tier 5 is a 23-item flat-9999-soul sentinel category, not a genuine 5th shop tier), and a conditionally-rendered `<img>` with `onError`-driven `imageFailed` state so a broken/blocked image degrades to the colored tile alone, never a broken-image icon. `.item-row`/`.item-row__button`/`.badge` class names were deliberately preserved as continuity hooks so existing app.test.tsx/mobile.spec.ts selectors and counts kept working unmodified through a pure restyle.
  `styles.css`: rewrote `.item-list` to a `grid` (`repeat(auto-fill, minmax(72px, 1fr))`, ~4-5 cards/row at 390px), `.item-row` to a `flex-column` grid cell, `.item-row__button` to a background-less vertical card button; added `.item-card__tile`(+3 slot variants)/`.item-card__image`/`.item-card__tier`/`.item-card__active-chip`/`.item-card__label`/`.item-card__cost`; added `.phase-panel`/`.phase-panel__title`/`.phase-panel__strip`; removed the now-dead `.item-row__image`/`.item-row__image--placeholder`/`.item-row__name` rules (no longer referenced by the rewritten ItemRow markup); updated `.item-row__running-total`'s color for legibility against the new `--panel-parchment-deep` background.
  No test file needed changes: because the `.item-row`/`.item-row__button`/`.badge` hooks and existing counts were preserved unmodified, `app.test.tsx` and `e2e/mobile.spec.ts` (including the ≥40px tap-target check and the blocked-image-host boot test) all passed as-is — a legitimate ticket outcome, not a skipped step; both files were reviewed and confirmed to need no edits rather than assumed.
  Manual real-browser visual check (temp Playwright spec + screenshot, not committed): confirmed at 390×844 — orange/green/purple slot tiles, roman-numeral corner ribbons (I-IV), a violet "ACTIVE" chip on active items, khaki name-label strips, both parchment phase panels rendering with no horizontal overflow. Broken images (no network in this sandbox) degrade cleanly to the colored tile with no broken-image icon, confirming the onError fallback works for real, not just in unit tests.
  README's UI section rewritten to describe the new phase-panel/item-card grid layout, the tier-5 fallback judgment call, and the active-chip/broken-image-degrade behavior.
  Verified: `npm run build` clean; `npx vitest run` 43 passed/2 skipped (unchanged count — no test edits needed); `npm run gate:heldout` OK (5 files, zero held-out references — UI-only change, generator untouched); `npx playwright test` 5/5 (existing specs, unmodified, including the ≥40px tap-target and blocked-image-host checks).

- [x] **T11 — "Ability Point Order" panel styled like in-game (user request 2026-09-01, from a 2nd reference screenshot)**
  - Goal: replace the current ability-order steps display with the game's timeline panel. Reference spec (authoritative — fires can't see the image; colors/type from DESIGN.md):
    - Navy panel (`--panel-navy`), rounded corners, heading "Ability Point Order" (display face, `--text-light`), sitting below the phase panels.
    - One row per ability (4), in the hero's ability order. Row left edge: the ability's icon (~40px square tile; `heroes.json` abilities now carry an `image` URL — orchestrator refetched 2026-09-01; broken/missing image degrades to the ability's initial letter on a `--badge-khaki` tile).
    - Each row is a full-width darker bar (`--row-navy`). The generated ability sequence (unlocks + upgrades interleaved) maps to N sequential columns shared across ALL rows: spend #k renders a marker in its ability's row at column k — so markers never vertically align unless simultaneous, exactly like the game.
    - Markers: **unlock** = violet diamond (`--diamond-violet`, a rotated-square badge with a small diamond glyph, no number); **upgrade step** = khaki pill (`--badge-khaki`, `--ink` text) showing "◆" + the upgrade step's AP cost — 1 for the 1st upgrade, 2 for the 2nd, 5 for the 3rd (in-game AP costs; our generator currently emits 2 upgrades per ability, so ◆5 may not appear — derive from upgrade index, don't hardcode the set).
    - Non-interactive. Badge text contrast ≥4.5:1. On 390px the panel's content may scroll horizontally INSIDE the panel (`overflow-x: auto`) if columns overflow; the page never scrolls horizontally.
  - Files: new `src/components/AbilityOrderPanel.tsx`, `src/components/BuildCard.tsx` (swap in the panel), `src/styles.css`, `src/test/app.test.tsx`, `e2e/mobile.spec.ts` (assert panel presence + no page overflow).
  - Acceptance: (1) panel renders per spec for Infernus and non-Infernus heroes; (2) unlock vs upgrade markers visually distinct with correct AP numbers by upgrade index; (3) shared-column layout verified in a jsdom test (marker for spend k carries a column attribute/style derivable in the test); (4) all suites green.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: `Ability` type (generator/types.ts) gained `image: string | null` (re-exported, along with the previously-internal `AbilityLevelStep`, from the generator barrel so the new component could import both). New `src/components/AbilityOrderPanel.tsx`: one `.ability-order-panel__row` per hero ability (always 4, in hero-listed order), each starting with a 40px `AbilityIcon` (image with `onError`→`imageFailed` state, same pattern as T10's `ItemRow`, falling back to the ability name's first uppercased letter on a khaki tile) followed by a CSS-grid `.ability-order-panel__track` sized to `build.ability_order.length` fixed-width columns shared identically across every row — each step's marker is positioned via inline `gridColumn: step.step`, so column k is the same physical position in every row regardless of which ability it belongs to (also exposed as `data-column` for test assertions, since jsdom doesn't compute real grid layout).
  Unlock markers (`--marker--unlock`) are a plain `::before`-drawn rotated 12px square (no text, `--diamond-violet`) — the "no number" requirement plus DESIGN.md's diamond-glyph description are the same shape rendered via CSS transform, no separate glyph character needed. Upgrade markers (`--marker--upgrade`) are a khaki pill showing `◆` + `UPGRADE_AP_COST[upgradeIndex]` (`[1, 2, 5]`, indexed by a per-row counter incremented across that ability's own steps, not hardcoded to "2 upgrades exist" — the ticket's own caveat that ◆5 may never appear given the generator's current 2-upgrades/ability output, confirmed true in this snapshot: every row shows exactly ◆1 then ◆2).
  `BuildCard.tsx` dropped the old `.build-card__abilities`/`.ability-order`/`.ability-order__step--{unlock,upgrade}` list rendering entirely in favor of `<AbilityOrderPanel abilities={abilities} abilityOrder={build.ability_order} />`; gained an `abilities: Ability[]` prop threaded from `App.tsx` as `selectedHero?.abilities ?? []` (hero abilities were already loaded, just not previously passed down).
  `styles.css`: removed the now-dead `.ability-order`/`.ability-order__step--*` rules, added `.ability-order-panel` (navy panel) → `.ability-order-panel__scroll` (`overflow-x: auto`, the panel-internal-only scroll the ticket requires) → `.ability-order-panel__row` (row-navy bar) → icon tile + grid track + the two marker variants, all via the existing DESIGN.md token set (`--panel-navy`, `--row-navy`, `--badge-khaki`, `--diamond-violet`, `--ink`, `--text-light`).
  `app.test.tsx`'s "3 other heroes" test updated its stale `.ability-order__step` selector to `.ability-order-panel__marker`; a new dedicated test renders the real Infernus app and asserts acceptance (2) and (3) directly against the deterministic fallback sequence (not hardcoded to Infernus specifically, just to the always-4-abilities/unlock-then-2-round-robin-upgrades shape `abilityOrder.ts` guarantees for every hero): exactly 4 rows; the first row's unlock marker sits at `data-column="1"` with empty text; its 2 upgrade markers sit at columns 5 and 9 reading "◆1"/"◆2"; the second row's unlock marker sits at column 2 (not 1), directly demonstrating the shared-column-but-not-shared-position behavior.
  `e2e/mobile.spec.ts`'s first test now also asserts the panel and exactly 4 rows are present, alongside its existing no-overflow check — the same test that already covers "page never scrolls horizontally" continues to do so with the new panel's internal `overflow-x:auto` in play.
  Manual real-browser screenshot (temp Playwright spec, deleted, not committed): confirmed the navy panel, khaki icon-fallback tiles (images blocked in this sandbox, degrading cleanly), and the diagonal-stepping diamond/pill columns exactly matching the "markers align only when simultaneous" behavior — no page overflow at 390×844.
  README gained a paragraph under the UI section describing the panel's shared-column layout, marker semantics, and the AP-cost-by-index judgment call.
  Verified: `npm run build` clean; `npx vitest run` 44 passed/2 skipped (was 43/2, +1 new); `npm run gate:heldout` OK (5 files, zero held-out references — UI-only change, generator's `abilityOrder.ts` untouched beyond the type gaining an unused-by-generator `image` field); `npx playwright test` 5/5.

- [x] **T12 — app-wide design cohesion pass per DESIGN.md (user request 2026-09-01: "make the UI beautiful")**
  - Goal: the ENTIRE app reads as one in-game artifact — not two game-styled panels inside a web dashboard. Apply DESIGN.md everywhere T10/T11 didn't touch: app background `--bg-abyss`; build header becomes the teal title bar (hero name + build name in display face, agreement chip riding it); HeroPicker, PersonalizationBanner, and ItemDetailSheet restyled with DESIGN.md tokens (detail sheet = parchment sheet, slide-up kept as the app's one orchestrated motion moment). No white surfaces anywhere.
  - Typography: add `@fontsource/baloo-2` + `@fontsource/nunito-sans` (npm devDeps, bundled — runtime stays offline; log the additions in PROGRESS.md per the standing rule), wire the roles/weights and fallback stacks exactly as DESIGN.md specifies.
  - Quality floor (DESIGN.md): `prefers-reduced-motion` respected, visible keyboard focus (2px teal outline), text contrast ≥4.5:1, 390×844 no page overflow, tap targets ≥40px.
  - Files: `src/styles.css`, `src/App.tsx`, `src/components/{HeroPicker,PersonalizationBanner,ItemDetailSheet}.tsx`, `src/main.tsx` (font imports), `package.json`, tests/e2e as needed.
  - Acceptance: (1) zero hardcoded colors outside the :root token block (grep `#[0-9a-f]` in styles.css finds only the token definitions); (2) fonts load offline from the bundle (build output contains the woff2 files; no external font URL anywhere); (3) reduced-motion media query present and kills transitions; (4) all suites green incl. e2e 5/5.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: Achieved with ZERO JSX changes to `HeroPicker`/`PersonalizationBanner`/`ItemDetailSheet`/`App.tsx` beyond `main.tsx`'s font imports — the whole pass is CSS + token additions, since the existing markup already had the right hooks. `.build-card` dropped its own solid background entirely; `.build-card__header` became a full-bleed teal (`--header-teal`) title bar with rounded top corners and `Baloo 2` display face, and `.phase-panel`/`.ability-order-panel` gained their own `margin: 12px 14px …` (replacing the parent's padding-based inset) so the parchment/navy panels visually float on the app's abyss background between them — directly matching DESIGN.md's "parchment panels floating on a near-black abyss" framing rather than reading as boxes nested in a box.
  Added 8 new `:root` tokens beyond DESIGN.md's own table (`--surface-dark`, `--surface-dark-border`, `--text-muted`, `--badge-core-bg/-text`, `--badge-not-core-bg/-text`, `--overlay-scrim`) for the handful of surfaces the reference screenshots don't cover (hero select, personalization banner, core/not-core badges, the detail-sheet backdrop scrim) — all in the same abyss-family near-black-green palette, never white, and still confined entirely to the `:root` block so acceptance (1) holds. Re-colored `.item-card__tier`'s ribbon background from a literal `rgba(0,0,0,0.55)` to `var(--ink)` + `opacity: 0.85` and several muted-text spots (`.item-detail-sheet__meta dt`, regular stat rows, archetype subtitle) from literal grays to `var(--ink)`/`var(--text-light)` + `opacity`, matching the pattern already established by T10's `.item-card__cost`.
  `ItemDetailSheet` restyled to a parchment sheet (`--panel-parchment`, `--ink` text); its close button and "Active"/"Passive" section headings (the latter promoted to `Baloo 2` display face, matching DESIGN.md's "panel headings" typography rule) recolored to read correctly against the new light background instead of the old dark-card palette.
  Fonts: installed `@fontsource/baloo-2`/`@fontsource/nunito-sans` as devDeps (registry was reachable from this sandbox — confirmed with `npm view` before installing) and imported only the `latin-*` weight files needed per DESIGN.md (Baloo 2 700/800; Nunito Sans 400/600/700) in `main.tsx`, JUDGMENT CALL: skipping the packages' other language subsets (cyrillic, greek, vietnamese, …) since the UI is English-only, keeping the added build payload to ~90KB across 10 files (woff2+woff per weight) rather than pulling every subset — confirmed via `npm run build`'s output listing and a grep of the built CSS/HTML for any `fonts.googleapis.com`/`fonts.gstatic.com`/external URL (none found; fully offline).
  Motion: added the detail sheet's slide-up + backdrop fade as plain CSS `@keyframes` (no JS animation library, no runtime dependency added) plus a `scale(1.03)` hover/press state on item cards (ability rows stay non-interactive per T11's own spec, so they don't get one); a `@media (prefers-reduced-motion: reduce)` rule collapses all animation/transition durations to near-zero app-wide. Added a real `:focus-visible` rule (2px `--header-teal` outline) — previously nothing overrode the browser default anywhere in the app.
  New `src/test/styles.test.ts` (4 tests) encodes T12's own acceptance criteria as regression tests instead of one-off manual checks: zero hex colors outside the `:root` block (parses the block via regex, strips it, asserts no hex remains in what's left), the reduced-motion media query's presence, `@fontsource` imports (not an external font host) referenced from `main.tsx`, and the focus-visible outline rule's presence. No other test file needed changes — no DOM structure changed, only CSS/tokens/font imports.
  Manual real-browser screenshots (temp Playwright spec, deleted, not committed): confirmed the teal title bar, floating parchment/navy panels on the abyss, and — with an item tapped — the parchment detail sheet with its backdrop scrim and slide-up all render correctly at 390×844, no white surfaces anywhere, no overflow.
  README gained a new "Design cohesion pass (T12)" subsection under UI, plus a judgment-call bullet under "Known data gaps" documenting the two new devDeps and why they don't violate the "zero runtime deps beyond react/react-dom" rule (fonts are build-time-only static assets, no runtime code).
  Verified: `npm run build` clean (build output confirmed 10 font files, zero external font/CSS host references); `npx vitest run` 48 passed/2 skipped (was 44/2, +4 new in styles.test.ts); `npm run gate:heldout` OK (5 files, zero held-out references — UI/CSS-only change, generator untouched); `npx playwright test` 5/5.

- [x] **T15 — real per-hero ability order (user bug report 2026-09-01: "ability upgrade order is the same across all heroes")**
  - Why: `buildAbilityOrder` ships the same round-robin fallback for the whole roster because every snapshot row had `sequence: null` — the upstream field is actually `abilities` (ability IDs in AP-spend order). The orchestrator has fixed the pipeline and recommitted snapshots: `ability_order_stats` and `high_badge_ability_order_stats` (Phantom+, badge ≥81) are now each ≤25 rows of `{ sequence: number[], wins, matches }`, pre-sorted by matches desc then wins desc, sequences typically ~15 IDs long.
  - Goal: the generator builds each hero's order from its own data. Source pick: `high_badge_ability_order_stats` if its top row has matches ≥ 100, else `ability_order_stats`. Row pick: highest matches (tie: highest wins, then ascending joined sequence — keep it deterministic, data is pre-sorted but re-sort defensively). Map the sequence to steps: first occurrence of an ability ID = `unlock`, later occurrences = `upgrade`; render the FULL sequence (all ~15 AP spends), not just 12. Sequence IDs must all resolve to the hero's 4 abilities — any unknown ID or empty/missing stats → keep the existing round-robin fallback (don't delete it; it stays test-covered).
  - HELD-OUT RULE: pick by matches/wins from analytics only — never consult Zergggy data.
  - UI: AbilityOrderPanel keeps its T11 design; column count now follows the real sequence length (~15) — horizontal scroll inside the panel on 390px stays. Upgrade AP-cost badges still derive from upgrade index (1st=1, 2nd=2, 3rd=5).
  - Files: `src/generator/abilityOrder.ts`, `src/generator/types.ts` (AbilityOrderStat.sequence: number[] | null; add high_badge_ability_order_stats), `src/generator/index.ts`, `src/components/AbilityOrderPanel.tsx` (or equivalent), `src/test/generator.test.ts`, `src/test/app.test.tsx`.
  - Acceptance: (1) snapshot-backed test: ≥3 heroes (include Infernus, hero_id 15) produce pairwise-different step sequences; (2) fixture test: unlock/upgrade kinds derived from first-vs-later occurrence; unknown ID in a fixture sequence falls back to round-robin; (3) determinism test green; (4) Infernus's rendered order equals its top-row sequence from the chosen source; (5) all suites green.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: JUDGMENT CALL — the ticket's own parenthetical says "Infernus, hero_id 15", but the snapshot data says Infernus is hero_id 1 (hero_id 15 is actually Bebop); used the real id (1) throughout rather than block on the ticket's typo, and noted the discrepancy in code/test comments.
  `abilityOrder.ts` rewritten: `buildAbilityOrder(abilities, abilityOrderStats, highBadgeAbilityOrderStats)` now picks the best row per source (`pickBestRow`: highest matches, tie-break highest wins, tie-break ascending joined sequence — re-sorts defensively rather than trusting upstream order), prefers the high-badge row when its matches ≥ 100 (`HIGH_BADGE_ABILITY_ORDER_MIN_MATCHES`), and maps the winning sequence to steps (first occurrence per ability id = unlock, later = upgrade). Falls back to the original deterministic round-robin (kept, unchanged) when no usable row exists or any id in the sequence doesn't resolve to one of the hero's 4 abilities. `index.ts` now computes `abilityOrder` once per hero (not per archetype) and threads it into `buildForArchetype` instead of calling `buildAbilityOrder(hero.abilities)` inline.
  `types.ts`: `AbilityOrderStat.sequence` is now `number[] | null` (was `unknown`); `HeroAnalytics` gained `high_badge_ability_order_stats: AbilityOrderStat[]`.
  For Infernus specifically: high-badge top row has matches=2948 (≥100 floor) so it wins; sequence resolves to Afterburn getting 3 upgrades (◆1/◆2/◆5, matching the AP-cost-by-index UI rule already in place) unlocking 1st, with Napalm/Flame Dash/Concussive Combustion unlocking at columns 5/3/7 respectively — confirmed against the real committed snapshot, not fabricated.
  Tests: `generator.test.ts` gained a snapshot-backed pairwise-difference test (Infernus/Seven/Vindicta all produce distinct ability-id sequences) and a snapshot-backed test asserting Infernus's build.ability_order exactly matches its chosen source's top-row sequence; a new `describe('buildAbilityOrder', …)` fixture suite (5 tests) covers unlock/upgrade-kind derivation, the unknown-id→fallback path, the empty/all-null→fallback path, the high-badge-floor gate (99 vs 100 matches), and the matches/wins/sequence tie-break order. `app.test.tsx`'s ability-order panel test rewritten to derive its expected columns/kinds/AP-costs from the real committed Infernus snapshot at test time (not hardcoded round-robin assumptions) so it stays correct if the snapshot data changes. `snapshots.test.ts` gained one line asserting `high_badge_ability_order_stats` is an array for every hero.
  README: replaced the "sequence is null, always fallback" paragraph in the generator section with the real T15 behavior, and removed the now-stale "known data gap" bullet about `ability_order_stats[].sequence` being null.
  Verified: `npm run build` clean; `npx vitest run` 55 passed/2 skipped (57 total, +8 new: 2 snapshot-backed generator tests, 5 fixture buildAbilityOrder tests, 1 rewritten app.test.tsx panel test); `npm run gate:heldout` OK, 5 files clean (had to reword one code comment that said "Zergggy" — the gate's regex is case-insensitive and scans comments too, not just imports); `npx playwright test` 5/5 (e2e specs don't touch ability-order DOM content, confirmed still green rather than assumed).

- [x] **T16 — remove the personal-insight block at the top of the app (user request 2026-09-01: "I don't care for the personal insight at the top")**
  - Goal: the top-of-page personal match-history insight section is gone entirely. If the only consumer of `src/personalization/` is that block, delete the module, its tests, its README section, and its CLAUDE.md repo-map line too. Per-build/per-item annotations elsewhere (if any) stay. `public/data/personal-matches.json` and the fetch pipeline stay untouched (orchestrator-owned; unused data is fine).
  - Files: `src/App.tsx`, the insight component, `src/personalization/` (likely delete), `src/test/` suites that reference it, README, CLAUDE.md repo map.
  - Acceptance: (1) no insight block renders at the top on any hero; (2) no dead imports/dead module left if unconsumed; (3) all suites green (personalization tests removed with the module).
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: confirmed `PersonalizationBanner` (rendered from `App.tsx`) was the ONLY consumer of `src/personalization/` (no per-build/per-item annotation exists elsewhere), so deleted the whole module: `src/personalization/index.ts`, `src/components/PersonalizationBanner.tsx`, `src/test/personalization.test.ts`. Removed `loadPersonalMatches`/`PersonalMatch` from `src/data/loaders.ts` (its only caller was the deleted banner) and all `insight`/`computeDurationInsight`/`PersonalizationBanner` state, imports, and JSX from `App.tsx`. Removed the now-dead `.personalization-banner` CSS rule from `styles.css`.
  `public/data/personal/matches.json` and its fetch-data.mjs pipeline left untouched (orchestrator-owned per the ticket) — the README's data-file table row was reworded to note the file is fetched but no longer consumed rather than removing the row, since the file still exists and is documented data.
  README: removed the "## Personalization (`src/personalization/`)" section entirely. CLAUDE.md: removed the `src/personalization/` repo-map line.
  Tests: added one assertion to the existing "shows Infernus by default" app.test.tsx test — `document.querySelector('.personalization-banner')` is null — making acceptance (1) an explicit regression test rather than only an implicit compile-time guarantee. The 4 personalization-only unit tests were removed with their module (acceptance 3).
  Verified: `npm run build` clean (47 modules, down from 49 — banner + its 1 import chain gone); `npx vitest run` 51 passed/2 skipped (53 total, was 55/2/57 pre-T16 — net -4 personalization tests, +1 banner-absence assertion); `npm run gate:heldout` OK (5 files, UI-only change, generator untouched); `npx playwright test` 5/5.

- [x] **T18 — builds must be buyable in-game: respect upgrade chains, catalog-only items (user bug report 2026-09-01: Infernus build buys Extra Spirit after Improved Spirit; shows "upgrade_stabilizing_tripod", not a real item)**
  - Data (already committed by orchestrator): `items.json` now contains ONLY real shop items (173, was 251 — disabled/non-shopable entries like upgrade_stabilizing_tripod are gone), and each item carries `components: number[]` (catalog ids of the items it upgrades from; buying the upgrade consumes the component, which then becomes unpurchasable in-game).
  - Generator rules: (1) every recommended item id must exist in the catalog — analytics rows whose item_id is not in items.json are skipped everywhere (score, buy order, permutations); (2) a build never contains two items from the same upgrade chain: if a picked item's `components` (follow transitively) or any item that lists it as a component is also picked, keep the higher-scoring one, drop the other, refill from the next-best candidate — deterministic (score desc, then ascending item id).
  - Files: `src/generator/` (score/index/types), `src/test/generator.test.ts`.
  - Acceptance: (1) fixture test: chain pair in candidates → exactly one survives, the higher-scoring, refill happens; (2) snapshot-backed test: no hero's build contains an id absent from items.json, and no build contains an item plus one of its components; (3) Infernus build specifically contains at most one of the Spirit chain; (4) determinism green; (5) all suites green.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: `Item` type (generator/types.ts) gained `components: number[]`. New `src/generator/itemChains.ts`: `buildItemChainGroups(items)` — union-find over the components graph (each item starts as its own singleton; every `components` edge (ignoring ids absent from the catalog) unions the two items) returning a `Map<itemId, Set<chainMemberIds>>` so both directions (an item's components, and anything that lists it as a component) land in the same group transitively, with path compression for efficiency.
  `index.ts`: `generateBuilds` now builds `catalogAnalytics` — `item_stats`/`high_badge_item_stats` filtered to ids present in `items.json` — before computing `itemStatsById`/`highBadgeStatsById`/`meanWinRate`/`maxMatches`/`maxHighMatches`, so an analytics row for a non-catalog item (e.g. the real "upgrade_stabilizing_tripod" case from the bug report) is invisible to scoring and buy-order everywhere, satisfying acceptance (2) by construction (only catalog items can ever be scored, so only catalog items can ever be picked). `chainGroups = buildItemChainGroups(items)` computed once and passed into both archetype calls. `buildForArchetype` gained a `blockedByChain` Set: `tryTake` marks every other member of a picked item's chain group as blocked; both the main greedy selection loop and the backfill loop skip blocked ids — so once the higher-scoring member of a chain is picked (items arrive pre-sorted score desc, tie ascending id), every other chain member is permanently excluded and the loop naturally continues to the next-best candidate, i.e. refill is inherent to the existing greedy pass rather than a separate step.
  Tests (`generator.test.ts`): fixed a pre-existing fixture object (`scoreItem high-elo blend`) that needed `components: []` added once `Item.components` became required. Two new snapshot-backed tests in the main suite: every one of the 38 heroes' generated builds contains only catalog item ids and never two items from the same chain group (iterates the full roster, not a sample, per the ticket's "no hero's build" wording); Infernus's build specifically contains at most one item from the real Extra/Improved/Boundless Spirit chain (chain root located via `items.find(item => item.name === 'Boundless Spirit')`, not hardcoded ids, so the test stays resilient to snapshot id reshuffling). New `describe('buildItemChainGroups', …)` (3 fixture tests: a simple 2-item chain unions correctly; unrelated items stay in singleton groups; a component id absent from the catalog is ignored rather than crashing). New `describe('generateBuilds: upgrade-chain exclusivity (T18)', …)` integration test: 5 fixture early-tier items with strictly decreasing match counts, one designated parent/child pair — without the chain rule the naive top-4 pick would be [parent, child, next, next], with the rule it is exactly [parent, next, next, next] (child dropped, next-best refills), directly exercising acceptance (1)'s exact scenario end-to-end through `generateBuilds` rather than only through the lower-level `buildItemChainGroups` unit.
  README: fixed a stale "251 items" reference (Known data gaps section) to "all items in the catalog"; reworded the `items.json` data-table row to say "ONLY real, currently-shopable items (173...)" and mention the new `components` field; added a paragraph to the generator section explaining catalog-only filtering and chain-exclusivity, referencing `itemChains.ts`.
  Verified: `npm run build` clean; `npx vitest run` 57 passed/2 skipped (59 total, was 51/2/53, +6 new: 2 snapshot-backed catalog/chain tests, 3 buildItemChainGroups fixtures, 1 chain-exclusivity integration test); `npm run gate:heldout` OK, 6 files clean (itemChains.ts is new and holds zero held-out references); `npx playwright test` 5/5 (existing specs don't assert on item-selection content, confirmed still green).

- [x] **T17 — desktop fills the viewport; mobile UI unchanged (user request 2026-09-01: "fill the screen on a mac viewport but maintain its good mobile ui on phone")**
  - Goal: on desktop widths the app stops being a narrow centered column and uses the screen. Breakpoint ~≥1024px: the layout goes fluid up to ~1440px max-width with comfortable margins; phase panels ("Early Game" / "Mid to Late Game") sit side by side when they fit, card grids grow their per-row count, the Ability Point Order panel spans full content width (no internal horizontal scroll when it fits). Below the breakpoint NOTHING changes — the 390×844 mobile experience is the reference and must stay pixel-equivalent.
  - DESIGN.md's Desktop note (added 2026-09-01) is authoritative for the breakpoint/max-width values.
  - Files: layout CSS (wherever T12 put it), `src/App.tsx` if structure needs a wrapper, `e2e/` — add a desktop spec at 1440×900 asserting no horizontal page scroll and content width ≥ 85% of viewport (capped by max-width), keep the mobile spec green. Update CLAUDE.md's "desktop is just a centered column" convention line to the new truth.
  - Acceptance: (1) 1440×900 e2e: content fills per above, no page-level horizontal scroll; (2) 390×844 e2e unchanged and green; (3) zero hardcoded colors added outside the :root token block (T12 rule holds); (4) all suites green.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: New `.build-card__phases` wrapper div (BuildCard.tsx) around the two `.phase-panel`s — a pure structural addition, no other JSX changed — lets desktop CSS lay them out as flex columns while mobile leaves them exactly as they were (a plain block, panels stack via their existing margin-based spacing, unchanged from T10/T12). No `App.tsx` changes were needed.
  `styles.css` gained one new `@media (min-width: 1024px)` block (the first breakpoint above the existing 481px tablet rule, so nothing below 1024px is touched): `.app` goes `max-width: 1440px` with `32px auto` margin and `48px` side padding (fluid fill up to the cap, comfortable margins per DESIGN.md); `.build-card__phases` becomes `display: flex` with a `14px` gap and each `.phase-panel` at `flex: 1` (equal columns), first/last child getting `margin-left`/`margin-right: 14px` so the outer edges match the existing 14px side-margin convention already used by `.phase-panel`/`.ability-order-panel` elsewhere — no new spacing values invented. The item-card grid (`repeat(auto-fill, minmax(72px, 1fr))`) and the Ability Point Order panel needed zero CSS changes: both already grow/span to fill whatever width their parent gives them, so wider phase-panel columns and a wider `.app` automatically satisfy "card grids grow their per-row count" and "Ability Point Order spans full content width" without new rules.
  New `e2e/desktop.spec.ts`: `test.use({ viewport: { width: 1440, height: 900 } })` overrides `playwright.config.ts`'s default 390×844 for this file only. 3 tests: no page-level horizontal overflow + `.app` width in `[900*0.85, 1440]` (covers acceptance's "≥85% of viewport, capped by max-width"); the two `.phase-panel`s sit at the same vertical position with the first ending before the second begins (side-by-side, not stacked); the ability-order panel's width is ≥85% of `.app`'s width (spans full content width). All 3 pass against the real production build.
  CLAUDE.md's "desktop is just a centered column" convention line rewritten to state both regimes (below/at-or-above 1024px). README's UI section's stale "Desktop is just the same layout centered in a ~480px column" sentence extended with the new T17 behavior and a DESIGN.md pointer.
  Verified: `npm run build` clean; `npx vitest run` 57 passed/2 skipped (unchanged — no unit-test-relevant DOM/logic changed, only a wrapper div + CSS); `npm run gate:heldout` OK (6 files, UI/CSS-only change, generator untouched); `npx playwright test` 8/8 (existing 5 mobile specs unchanged and green at 390×844, +3 new desktop specs at 1440×900) — zero hardcoded colors added (the new CSS block uses only existing `var(--...)` tokens and literal pixel/flex values, no new hex).

- [x] **T19 — tune generator constants toward Zergggy agreement (user request 2026-09-01: "adjust the algorithm numbers until we have more agreement with the Zergggy build")**
  - Zergggy is now the TUNING set (see header rule). Data: `public/data/zergggy/matches.json` via the existing `src/validation/` scoring.
  - Goal: a deterministic offline sweep harness — `scripts/tune-generator.mjs` (or `src/validation/tune.ts` + npm script `tune`) — that (1) enumerates the generator's actual scoring constants (win-rate vs usage weights, damping K, high-badge blend cap/ramp, phase thresholds — read `src/generator/score.ts` and list what exists, don't invent), (2) grid-searches a bounded space (≤ ~500 combos), (3) for each combo generates the Infernus build and computes Zergggy agreement %, (4) picks argmax with deterministic ties (fewest changes from current values, then lexicographic parameter order).
  - Apply the winning constants to `src/generator/` as plain numbers (a named-constants block is fine). The harness may import from `src/validation/`; `src/generator/` still must not (gate:heldout stays green).
  - HARD LIMIT: the harness and this ticket must NEVER read or reference `public/data/heldout-ctc/` — that is the held-out set.
  - Sanity floor: after applying the winner, every hero still exports a valid build (≥12 items, 4 abilities), determinism green, T18's chain rules hold. If the argmax breaks sanity, take the best combo that passes it.
  - Acceptance: (1) PROGRESS.md records baseline agreement, tuned agreement (must be ≥ baseline), the grid searched, and the winning constants; (2) `npm run tune` (or documented command) reruns reproducibly; (3) all suites + gates green.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: `src/generator/score.ts` refactored to a `ScoreConstants` interface + `DEFAULT_SCORE_CONSTANTS` object covering all 9 tunable numbers that actually existed (confidenceK damping, highBadgeMinSample/highBadgeWeight blend ramp, the 4 composite weights winWeight/useWeight/valueWeight/biasWeight, and archetypeBias's vitalityBias/offArchetypeBias) — nothing invented beyond what was already hardcoded. Every scoring function (`highBadgeBlendWeight`, `blendHighBadgeStat`, `dampedWinRate`, `archetypeBias`, `scoreItem`) gained an optional trailing `constants: ScoreConstants = DEFAULT_SCORE_CONSTANTS` parameter — zero behavior change for existing callers since the default equals the prior hardcoded values. `src/generator/index.ts`'s `generateBuilds`/`buildForArchetype` thread an optional `constants` parameter through to `scoreItem`; `App.tsx` and all other callers are unaffected (they never pass it). Back-compat `HIGH_BADGE_MIN_SAMPLE`/`HIGH_BADGE_WEIGHT` named exports kept, derived from `DEFAULT_SCORE_CONSTANTS`.
  Harness: new `scripts/tune-entry.ts` (outside `src/generator/`) re-exports `generateBuilds`/`DEFAULT_SCORE_CONSTANTS`/`buildItemChainGroups` from `../src/generator` and `validateBuildAgainstMatches` from `../src/validation` — the one and only bridge between generator and validation, so `gate:heldout`'s isolation guarantee on `src/generator/` itself is untouched. `scripts/tune-generator.mjs` uses Vite's Node `build()` API (already an explicit devDependency, no new package) in library mode to bundle that entry once into a gitignored `.tune-tmp/tune-entry.mjs`, dynamically imports it, then runs an in-process, no-rebuild-per-combo grid sweep: `confidenceK`×`highBadgeMinSample`×`highBadgeWeight` (3 candidates each, independently) × two named profiles (`weightsProfile`: default/winHeavy/useHeavy; `archetypeBiasProfile`: default/looser/tighter) — 3×3×3×3×3 = 243 combos, within the ≤500 bound. `isBetter` picks argmax agreement, tie-broken by fewest dimensions changed from the current `DEFAULT_SCORE_CONSTANTS` values then by an explicit `DIMENSION_ORDER` lexicographic index order, exactly per the ticket's tie-break spec. `sanityCheckAllHeroes` re-runs `generateBuilds` for all 38 heroes per candidate (≥12 items, exactly 4 abilities, no duplicate items, no two chain-mates both picked via `buildItemChainGroups`) and `main()` walks the sorted results taking the first sanity-passing one, implementing the "if the argmax breaks sanity, take the best combo that passes it" fallback. `npm run tune` added to package.json.
  Caught and fixed a bug before finalizing: the grid arrays were initially ordered with index 0 ≠ the actual `DEFAULT_SCORE_CONSTANTS` value for the three scalar dimensions (e.g. `confidenceK: [25, 50, 75]` while the real default is 50), so `changesFromBaseline`'s "index 0 is the baseline" assumption was silently wrong — the harness ran fine and picked a real winner, but its own "changes from baseline: 0" reporting was inaccurate (the true winner differed from baseline in 3 fields, not 0). Fixed by reordering each scalar dimension's candidate list so index 0 matches the true current default (`confidenceK: [50, 25, 75]`, `highBadgeMinSample: [100, 50, 150]`, `highBadgeWeight: [0.75, 0.6, 0.9]`), then re-ran; the corrected run reports the true tie-break state.
  Sweep result: baseline (current `DEFAULT_SCORE_CONSTANTS`) Infernus/Zergggy agreement = 43%. Winner = 46% agreement, exactly 1 change from baseline: the `archetypeBiasProfile` moved from `default` (vitalityBias 0.6, offArchetypeBias 0.2) to `looser` (vitalityBias 0.7, offArchetypeBias 0.3); every other constant (confidenceK=50, highBadgeMinSample=100, highBadgeWeight=0.75, and all 4 composite weights) stayed at baseline. Passed `sanityCheckAllHeroes` on the first candidate tried (no fallback needed). Applied: `score.ts`'s `DEFAULT_SCORE_CONSTANTS.vitalityBias`/`offArchetypeBias` updated to 0.7/0.3 as plain numbers.
  README's composite-score-formula code block updated (`0.6 vitality, 0.2 off-archetype` → `0.7 vitality, 0.3 off-archetype`, noting the T19 tune). No test hardcoded the old archetype-bias numeric values (`archetypeBias` has no direct unit test; `generator.test.ts`'s existing `HIGH_BADGE_MIN_SAMPLE`/`HIGH_BADGE_WEIGHT` tests reference those constants by name, unaffected since neither changed).
  Verified: `npm run build` clean; `npx vitest run` 57 passed/2 skipped (unchanged — refactor is behavior-preserving for every non-harness caller); `npm run gate:heldout` OK (6 files, harness lives outside `src/generator/`); `npx playwright test` 8/8 (build-content-independent specs, confirmed still green rather than assumed).

- [x] **T20 — held-out test: agreement with ctc's Drifter builds (user request 2026-09-01; run AFTER T19 lands)**
  - Data (committed by orchestrator): `public/data/heldout-ctc/matches.json` — player "ctc" (NA leaderboard rank 2, account 1294549649), their 30 most recent Drifter (hero 64) matches with shop purchases; same shape as zergggy/matches.json.
  - Goal: extend `src/validation/` with a ctc core-set + agreement scoring (same ≥30% core-set rule), compute agreement of our exported Drifter build, display the agreement chip on Drifter exactly like Infernus's Zergggy chip (label it with the player name), and record the number in PROGRESS.md as a FINDING.
  - EXPERIMENT INTEGRITY: this number must never drive any generator change — no follow-up "fix" commits touching `src/generator/` in response to it. Low agreement is a finding. gate:heldout enforces the string ban in `src/generator/`.
  - Files: `src/validation/`, the component rendering the agreement chip, `src/test/validation.test.ts`, README (validation section).
  - Acceptance: (1) Drifter shows a ctc agreement chip; (2) Infernus's Zergggy chip unchanged; (3) PROGRESS.md reports the % as a finding; (4) all suites + gates green.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: `src/validation/loadMatches.ts`'s single `fetchZergMatches` split into a shared internal `fetchMatches(path)` helper plus `fetchZergMatches()` (unchanged path/behavior) and a new `fetchCtcMatches()` (`data/heldout-ctc/matches.json`). `src/validation/index.ts` gained `validateBuildsAgainstCtc(builds)` — identical shape/logic to the existing `validateBuildsAgainstHeldOut`, just sourced from ctc's matches — since `coreSet.ts`/`orderPreferences.ts`/`validateBuild` are already fully generic over any `ZergMatch[]` sample, nothing in the scoring math needed to change. `src/constants.ts` gained `DRIFTER_HERO_ID = 64` (matching `scripts/fetch-data.mjs`'s existing `HELDOUT_CTC_HERO_ID`).
  `App.tsx`: added a `validationLabel: string | null` state (null = Infernus's existing unlabeled chip, unchanged); a new `else if (hero.id === DRIFTER_HERO_ID)` branch calls `validateBuildsAgainstCtc` and sets the report + `'ctc'` label. `BuildCard.tsx` gained an optional `validationLabel?: string | null` prop; the chip renders `{agreement_percent}% agreement{label ? \` (${label})\` : ''}` — Infernus's rendered text is byte-for-byte unchanged since its label stays null (acceptance 2), Drifter's reads "NN% agreement (ctc)" (acceptance 1).
  Tests: extended `validation.test.ts`'s held-out isolation regex from `/zergggy/i` to `/zergggy|heldout[-_]?ctc/i` (matching `gate:heldout`'s own regex, which the orchestrator had already widened) — this initially caught two of my own doc comments that said "Zergggy" in `App.tsx`/`BuildCard.tsx` (same recurring gate-vs-comment class of mistake as T15/T19), fixed by rewording to "tuning-set". Added a new snapshot-backed test mirroring the existing Infernus one, for a real generated Drifter build against real ctc matches (bounds-only assertions, no fixed expected %, per EXPERIMENT INTEGRITY). `app.test.tsx` gained a dedicated test switching to Drifter and asserting both chips: Infernus's stays exactly `/^\d+% agreement$/` (no label) and Drifter's matches `/% agreement \(ctc\)/`; the existing "3 other heroes" test was unaffected since it samples the first 3 non-Infernus hero-select options by DOM order (ids 2/3/4), never reaching Drifter (id 64).
  Finding (never fed back into `src/generator/`): Drifter's generated build agrees **47%** with ctc's real Drifter matches (core-set ≥30% win-weighted rule, same formula as Zergggy's). Computed once via a throwaway harness reusing `scripts/tune-entry.ts`'s bundle (script deleted immediately after, not committed).
  README: intro paragraph, the data-pipeline table, the generator section's gate description, and the "Held-out validation" section rewritten for accuracy post-T19/T20 (Zergggy = tuning set, ctc = held-out test set) — was previously stale/self-contradictory since T19 already tunes toward "held-out" Zergggy. Left the "Known data gaps"/"Acceptance checklist" sections' historical percentages/phrasing untouched (frozen point-in-time records, not live documentation).
  Verified: `npm run build` clean; `npx vitest run` 59 passed/2 skipped (61 total, +2: one Drifter/ctc snapshot test, one app.test.tsx chip-labeling test); `npm run gate:heldout` OK (6 files, `src/generator/` untouched by this ticket); `npx playwright test` 8/8.

- [x] **T24 — PWA caching of item images for true offline visuals (pulled from ROADMAP.md's known gaps/stretch list, queue was empty)**
  - Goal: a repeat visit shows real item/hero artwork even when the external image host is unreachable, instead of falling back to the colored-tile/broken-image state every time.
  - Approach: hand-written service worker (`public/sw.js`, no new npm dependency, static frontend stays zero-runtime-dep) — cache-first for `GET` requests whose `destination === 'image'` only; everything else (app shell, `/data/**` snapshots) untouched. Registered from `src/main.tsx` via a small, guarded `registerServiceWorker()` helper (no-op when `serviceWorker` isn't in `navigator`, e.g. jsdom tests).
  - Files: `public/sw.js`, `src/registerServiceWorker.ts`, `src/main.tsx`, `src/test/registerServiceWorker.test.ts`, `e2e/offline-images.spec.ts`, README.
  - Acceptance: (1) a real Playwright run shows a previously-loaded item image still rendering after the image host is blocked; (2) app still boots fine with no service worker (existing offline-resilience test stays green); (3) zero new npm dependencies; (4) all suites + gates green.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: `public/sw.js` is a small hand-written service worker — `install` calls `skipWaiting()`, `activate` deletes any stale-named cache then calls `clients.claim()` (so a page load right after a redeploy is claimed immediately rather than waiting for a second reload), and the `fetch` handler only intercepts `GET` requests where `request.destination === 'image'` (returns/falls through for everything else, so the app shell and `/data/**` JSON snapshots are untouched and rely on the normal browser HTTP cache). Cache-first: on a hit, serve straight from `caches.open('deadlock-images-v1')`; on a miss, fetch from the network, cache the response, and return it; on a network failure, fall back to whatever was cached (`undefined` if nothing was). `src/registerServiceWorker.ts` is a tiny guarded helper — no-op when `'serviceWorker' in navigator` is false (jsdom has no such API, so unit tests are unaffected) — registering `sw.js` on `window`'s `load` event, `.catch()`-swallowed since the app must work fully with no service worker. Wired into `src/main.tsx` right after the React root render, as a fire-and-forget call.
  Bug caught during verification (not obvious from reading the code alone): the first version gated caching on `if (response.ok) cache.put(...)`. Every `<img>` this app renders is cross-origin (`assets-bucket.deadlock-api.com`) with no `crossorigin` attribute, so the `Response` the service worker's own `fetch(request)` receives for it is **opaque** — per the Fetch API, an opaque response always reports `status: 0` and `ok: false`, even for a genuine 200. That guard therefore silently skipped `cache.put()` for every single image, real-world or mocked, while still returning the (correctly loaded) image to the page — the bug was invisible from the rendered UI and only surfaced by directly inspecting `caches.keys()`/`cache.keys()` after a reload and finding the cache permanently empty. Fixed by dropping the `.ok` gate entirely: `cache.put()` now runs on any response that comes back from `fetch()` at all, and only an actual thrown network error (the `.catch()`) skips caching.
  `e2e/offline-images.spec.ts` (new): three navigations against a mocked image host (`context.route()`, not `page.route()` — a service worker's own internal `fetch()` calls aren't attributed to a page/frame, so only context-level routing sees them) — (1) initial load, not yet controlled, image renders via the network; (2) reload, now controlled by the activated worker, its image fetch is cached; (3) reload with the mocked route switched to `route.abort()` (image host fully unreachable) — the previously-cached image still renders with `naturalWidth > 0`. Real network traffic to the actual image host was ruled out for this test: a direct `curl` against `assets-bucket.deadlock-api.com` from this sandbox returned `CONNECT tunnel failed, response 403` (the org egress proxy blocks it outright, beyond the two hosts CLAUDE.md's SANDBOX EGRESS note names), so a "let the network run for real, only block at the end" design (matching the existing `mobile.spec.ts` offline-resilience test) would fail for the wrong reason here; mocking via `route.fulfill()` for the first two phases is also arguably the more correct/deterministic design regardless of sandbox egress.
  README gained a new "Offline image caching" subsection (in the UI section, alongside the layout/ability-panel writeups) covering the design and the opaque-response subtlety; the "Parked / not done" list's PWA-caching entry was removed with a note that it's now done.
  Verified: `npm run build` clean; `npx vitest run` 61 passed/2 skipped (63 total, +2: `registerServiceWorker.test.ts`'s no-API no-op case and its registration-on-load case); `npm run gate:heldout` OK (6 files, `src/generator/` untouched by this ticket); `npx playwright test` 9/9 (8 existing specs unchanged and green, +1 new `offline-images.spec.ts`, confirmed via a standalone diagnostic script before the real spec was trusted — first run against a stale `dist/sw.js` from before the fix falsely still showed an empty cache, caught by rebuilding and rerunning before concluding the fix worked).

- [x] **T21 — Ability Point Order panel: no scrollbar, full-width row bars (user bug report 2026-09-01)**
  - User dislikes the horizontal scrollbar on the ability panel, and scrolling reveals the navy row bars stop short of the content's full width.
  - Goal: at 390×844 the whole ~15-column sequence FITS inside the panel with no horizontal scrollbar — shrink the per-column width/markers/tiles as needed (columns can go ~20px; unlock diamonds and AP pills scale down; ability icon tiles may shrink or the label column narrows). Do not clip or drop steps, do not reintroduce page-level scroll.
  - Row-bar bug (fix regardless): each `--row-navy` bar must span the panel content's full scrollable width, not just the visible viewport (size backgrounds on the scroll-content element, e.g. width: max-content/min-width: 100% on the row, not the scroll container). If any viewport still overflows (very long sequences, narrow desktop windows), bars must extend to the end of the scrolled content.
  - Files: `src/components/AbilityOrderPanel.tsx` + its styles, `src/test/app.test.tsx`, `e2e/mobile.spec.ts`.
  - Acceptance: (1) e2e at 390×844: ability panel `scrollWidth <= clientWidth` (no horizontal overflow) with a full 15-step sequence; (2) all 15 steps present in the DOM; (3) jsdom or e2e check that row bars are laid out on the full-width content element (assert the CSS structure); (4) all suites green.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: Measured the real worst case first rather than trusting the ticket's "~15" estimate: a throwaway harness (reusing `scripts/tune-entry.ts`'s bundle, deleted after use) ran `generateBuilds` for all 38 heroes and confirmed the true max `ability_order.length` is exactly 15, on Infernus — the app's default hero, so the common case is also the worst case.
  `AbilityOrderPanel.tsx`: `COLUMN_WIDTH` shrunk from 26 to 16 and a new `COLUMN_GAP = 1` constant added; the track's inline `width` style, which previously only counted `totalSteps * COLUMN_WIDTH` and silently ignored the CSS grid's inter-column gaps (a pre-existing inconsistency, not itself the reported bug — the browser was already rendering the true, larger content width regardless of the wrong declared value), now correctly computes `totalSteps * COLUMN_WIDTH + (totalSteps - 1) * COLUMN_GAP`. `styles.css`: `.ability-order-panel__track`'s `gap` matched to 1px; marker height/font-size/padding and the unlock diamond size all reduced (20px→18px, 0.65rem→0.55rem, 12px→8px diamond, tighter upgrade-pill padding) so `◆1`/`◆2`/`◆5` still read cleanly at the smaller column width — confirmed visually via a real-browser screenshot before trusting it, not just computed measurements.
  Row-bar fix: `.ability-order-panel__row` gained `width: max-content; min-width: 100%` — exactly the ticket's own suggested approach. Root cause: `.ability-order-panel__scroll` is a column flexbox with the default `align-items: stretch`, so each row (itself a block-level flex container) was being stretched to the *visible* scroll-container width regardless of its content's true width; once its track content overflowed that box, the row's own navy background stopped exactly at the visible edge instead of following the scrolled content, matching the reported bug exactly. `max-content` lets the row grow to fit its actual content when it's wider than the panel (so the background always follows an overflow, present or hypothetical); `min-width: 100%` keeps it filling the panel when content is narrower.
  Verified via direct DOM measurement in a real Chromium page (temporary Playwright scripts, deleted after use, not committed) before trusting the fix: at 390×844 Infernus's panel (`.ability-order-panel__scroll`) now measures `scrollWidth === clientWidth` (318 === 318, no scrollbar) with the full 15-step sequence; forcing a narrower viewport (250px, to actually exercise the overflow path) showed `row.offsetWidth` (310) matching the scroll container's `scrollWidth` (310) rather than its `clientWidth` (178) — confirming the row-bar background now does follow the overflowed content instead of stopping short.
  Tests: `src/test/styles.test.ts` gained a regression check (reads the raw CSS, same pattern as T12's checks) asserting `.ability-order-panel__row` contains both `width: max-content` and `min-width: 100%`. `src/test/app.test.tsx`'s existing T15 ability-panel test gained an explicit total-marker-count assertion against the real resolved sequence length, so a future regression that silently drops steps while shrinking columns would fail loudly. `e2e/mobile.spec.ts` gained a new real-browser test asserting `scrollWidth <= clientWidth` on the panel's scroll element and exactly 15 markers present, at 390×844 with Infernus's real (already-known-worst-case) sequence.
  README's Ability Point Order paragraph rewritten: the stale "◆5 never appears yet" claim (already inaccurate since T15's real per-hero data landed — some heroes' real sequences do reach a 3rd upgrade) corrected, and the new fixed column width / no-scrollbar-in-the-observed-max-case / row-bar-follows-overflow behavior documented.
  Verified: `npm run build` clean; `npx vitest run` 62 passed/2 skipped (64 total, +1 styles test); `npm run gate:heldout` OK (6 files, UI-only change, generator untouched); `npx playwright test` 10/10 (9 existing specs unchanged and green + 1 new ability-panel-specific mobile spec).

- [x] **T23 — synergy + affinity scoring, heavier usage/win-rate weighting, full re-tune (user request 2026-09-01T20:04Z: "agreement still far too low; factor in item usage and win rates more; consider character play styles and items that synergize with their abilities")**
  - Data (committed by orchestrator 2026-09-01): each `analytics/hero-N.json` gains `item_pair_stats` — top 200 Phantom+ item PAIRS by matches, `{ items: [idA, idB], wins, matches }` — which items win TOGETHER on this hero. `items.json` gains `roster_usage_share: number|null` — the roster-average usage share for the item (this hero's own share ÷ roster share = how much the item over-indexes on this hero's kit; that ratio IS the empirical play-style/ability-synergy signal).
  - Three additions to `src/generator/score.ts`, all behind `ScoreConstants` so the sweep can tune them:
    1. **Heavier usage/win-rate**: extend the tuning grid's weight profiles with options that weight usage and win rate substantially higher than the current defaults — the user asked for this explicitly; let the sweep confirm how far to push.
    2. **Hero-affinity multiplier**: `affinity = clamp(heroUsageShare / max(roster_usage_share, 0.01), 0.5, 3)`; `score *= 1 + affinityWeight * (affinity - 1)`; grid `affinityWeight ∈ {0, 0.15, 0.3}`.
    3. **Pair synergy in build assembly**: when picking the next item, bonus = `pairSynergyWeight * mean(lift(picked, candidate))` where lift = pair win rate − hero average win rate, damped toward 0 by pair matches (shrink-to-mean K, reuse the existing damping helper), 0 for unseen pairs; grid `pairSynergyWeight ∈ {0, 0.1, 0.2}`. Deterministic (stable tie-breaks unchanged).
  - Types/loaders: add the two fields where the Item/HeroAnalytics types live; loaders are generic passthrough.
  - Re-run `npm run tune` with the expanded grid. Keep the search bounded (≤ ~1500 combos; coarse-then-fine two-stage is fine if runtime demands). Apply the argmax (same deterministic ties + sanity floor as T19).
  - HARD LIMITS: never read/reference `public/data/heldout-ctc/`; gate:heldout green; determinism + T18 chain rules + T13 single-build all hold.
  - Also report CONTEXT (validation-side, cheap): Zergggy's self-agreement — mean pairwise core-set agreement across his own 30 matches — as the practical ceiling for this metric; a single fixed build can't beat a player's own match-to-match variance.
  - Acceptance: (1) tuned Zergggy agreement ≥ the current 46%, both numbers in PROGRESS.md with the winning constants and grid size, plus the self-agreement ceiling; (2) fixture tests for affinity multiplier and pair-synergy bonus (including the damped/unseen-pair zero case); (3) all suites + gates green.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: `src/generator/types.ts` gained `Item.roster_usage_share: number | null`, a new `ItemPairStat { items: [number, number], wins, matches }` interface, and `HeroAnalytics.item_pair_stats: ItemPairStat[]` — pure passthrough, `src/data/loaders.ts` needed zero changes since it already imports these types straight from `../generator`. `ScoreConstants` gained `affinityWeight`/`pairSynergyWeight` (both default 0 = no-op for every caller that doesn't pass them).
  `score.ts` gained two pure functions: `heroAffinityMultiplier(heroUsageShare, rosterUsageShare, constants)` — exactly the ticket's formula, clamped `[0.5, 3]`, null roster share falls back to the 0.01 floor — wired into `scoreItem`'s return as a final multiplier (reusing `useScore`, i.e. the existing blended usage ratio, as `heroUsageShare`, so no new stat computation needed); and `pairLift(pairStat, meanWinRate, constants)` — proven algebraically that `dampedWinRate(rate, matches, mean, K) - mean` is exactly damping the rate-minus-mean toward 0, so it's implemented as one call into the existing `dampedWinRate` rather than a second damping formula (satisfies "reuse the existing damping helper" literally); returns exactly 0 for a null/zero-matches pair stat.
  Pair synergy required restructuring `index.ts`'s `buildForArchetype`: the prior single static-sorted pass couldn't work since the bonus depends on what's already picked. Replaced with an incremental greedy loop (`runGreedyPass`) that, at each pick, rescans every remaining eligible candidate, computes `effectiveScore = staticScore + pairSynergyBonus(candidate, alreadyPicked)` (via a new `pairSynergyBonus` helper using `pairLift`, mean over all currently-selected items), and takes the max — a candidate only replaces the running best on a strictly higher score, so ties fall back to the original static-score/id order, meaning `pairSynergyWeight = 0` reproduces the exact old static-only ordering byte-for-byte (verified by a dedicated test). `generateBuilds` filters `item_pair_stats` to catalog-only pairs (same T18 pattern as `item_stats`) and builds a `pairStatsByKey` map once, passed to both archetype calls. Cost: ~156 items × ~13 picks × up to ~12 lift lookups per pick — a few thousand operations per archetype pass, negligible even across the tuning sweep (which only generates Infernus's build per grid combo).
  Grid: adding `affinityWeight`(3) × `pairSynergyWeight`(3) directly to the existing 243-combo T19 grid would hit 2187 combos (over the ~1500 bound), so `tune-generator.mjs` became a coarse-then-fine two-stage sweep as the ticket explicitly allows: stage 1 is the T19 grid shape unchanged in structure but `weightsProfile` gained two "heavier usage/win-rate" options (`winUseHeavy`: 0.4/0.4/0.1/0.1, `useMax`: 0.3/0.5/0.1/0.1) per the user's explicit request — 405 combos, affinity/pairSynergy held at 0. Stage 2 fixes stage 1's winning base constants and sweeps only `affinityWeight` × `pairSynergyWeight` (9 combos) on top. Total 414 combos, well under the bound.
  Sweep result: baseline (pre-T23 `DEFAULT_SCORE_CONSTANTS`) agreement = 46%. Stage 1 winner stayed at 46% (weight-profile/damping dimensions found no improvement over the existing T19 winner). Stage 2 winner: agreement **48%** at `affinityWeight=0.3, pairSynergyWeight=0` — the hero-affinity multiplier alone raised agreement 2 points; pair synergy found no improvement at any grid value on top of that affinity level, so it ships at 0 (implemented, tunable, but currently a no-op). Applied `DEFAULT_SCORE_CONSTANTS.affinityWeight = 0.3` (all other constants unchanged from T19's values).
  Self-agreement ceiling (CONTEXT, never fed back into `src/generator/`): computed via a throwaway harness (bundled `src/validation`'s `computeCoreSet`/`buildOrderPreferences`/`validateBuild` through the same Vite-library-mode bridge pattern as `tune-entry.ts`, deleted after use, not committed) — scored each of Zergggy's own 30 matches as a pseudo-build against the core set/order preferences derived from all 30, then averaged: **74.6%** mean (individual matches ranged 45%–92%), confirming a single fixed build can't realistically clear the high-70s/low-80s against this metric given the player's own match-to-match variance.
  Tests: `generator.test.ts` gained a `heroAffinityMultiplier` describe (identity at affinity 1, weight-0 no-op, both clamp directions, null-roster fallback) and a `pairLift` describe (unseen/zero-matches pair → 0, thin-sample damped near 0, high-sample approaches true lift, exact equality with the `dampedWinRate`-minus-mean formula) plus a `generateBuilds: pair synergy influences item choice` describe with two fixture-driven tests: one showing a strong recorded pair partner beating a marginally-higher-static-score item once `pairSynergyWeight > 0`, and one proving `pairSynergyWeight = 0` reproduces the identical static-order build. Existing `Item`/`HeroAnalytics` fixture builders across the file updated with the two new required fields (`roster_usage_share: null`, `item_pair_stats: []`) to keep them type-valid.
  README: composite-score formula block updated to show the affinity multiplier wrapping the composite; a new "Hero-affinity and item-pair synergy (T23)" subsection explains both signals and their current tuned values; the data-pipeline table's `items.json`/`analytics` rows gained the two new fields (and its stale 173-item count corrected to the current 156, a pre-existing drift noticed while editing that row, not itself part of this ticket's scope); the validation section gained a "Self-agreement ceiling" paragraph reporting the 74.6% figure.
  Verified: `npm run build` clean; `npx vitest run` 74 passed/2 skipped (up from 64 — +10 new T23 fixture tests); `npm run gate:heldout` OK (6 files in `src/generator/`, clean of held-out references — double-checked no "Zergggy" landed in any new `score.ts` comment, the recurring gate-vs-comment mistake from prior tickets); `npx playwright test` 10/10 (unchanged UI, generator-only ticket).

- [x] **T25 — candidate usage floor: never build items the hero's players don't buy (user bug report 2026-09-01T20:45Z: Kelvin build includes Lightning Scroll, which is impossible to trigger on him and which almost nobody buys)**
  - Root cause (orchestrator-verified in the committed data, do not re-derive): Lightning Scroll (id 493591231) has NO row at all in `analytics/hero-12.json` `item_stats` — zero recorded Kelvin matches. `scoreItem` feeds matches=0 into `dampedWinRate`, which shrinks fully to the hero MEAN win rate (0.555 for Kelvin), so an item nobody buys scores like an average-win-rate item; usage weight can't save it (T23's heavier win-rate profile amplified this), and the affinity clamp floor (0.5 → ×0.85) barely dents it.
  - Fix in `src/generator/` (eligibility, not re-weighting): add `minUsageShare` to `ScoreConstants` (default 0.01). An item whose blended hero usage share (`usageRatio` from `blendHighBadgeStat`; 0 when there is no stats row) is below the floor is INELIGIBLE — filtered out before build assembly / the greedy pass, not merely down-scored. Leave `dampedWinRate` itself unchanged (its shrink-to-mean is correct for thin-but-real samples and is reused by `pairLift`).
  - Starvation guard: measured on Kelvin, floor 0.01 keeps 89 of 156 items (0.02 keeps 78), so normal builds never starve; still, if any category/phase has fewer eligible items than slots, fill the remainder from below-floor items in the existing stable score order. Determinism and stable tie-breaks unchanged.
  - Tests: (a) fixture — an item with no stats row never appears in a generated build; (b) below-floor item excluded, at-floor item eligible; (c) starvation fallback works; (d) roster sweep — generate builds for all 38 heroes from the real snapshots and assert every chosen item has hero usage share ≥ floor (or is a starvation fallback, which the sweep should find zero of).
  - Report: re-run `src/validation/` and put the new Zergggy agreement number in PROGRESS.md next to the prior 48%. If it regresses below 46%, re-run `npm run tune` with the existing T23 grid and apply the argmax per the usual rules.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  Outcome: `ScoreConstants` gained `minUsageShare` (default `0.01`, documented rationale + a pointer to the starvation fallback in its doc comment). `index.ts`'s `buildForArchetype` now recomputes each candidate's blended `usageRatio` (via `blendHighBadgeStat`, same inputs `scoreItem` already uses internally — not a second stat computation) alongside its static score, and `isEligible` gained a hard `usageRatio < minUsageShare` filter gated by a new `allowBelowFloor` flag. `runGreedyPass` takes that flag; the two existing passes (quota-respecting fill, then any-tier backfill to `MIN_TOTAL_ITEMS`) now run with the floor enforced, followed by two more passes — identical targets, `allowBelowFloor: true` — that only ever add items if a phase is still short, implementing the starvation guard exactly as specced (each pass is a no-op unless the prior one left a gap).
  Root-cause bug confirmed fixed: Kelvin's real build no longer contains Lightning Scroll (id 493591231, 0 recorded matches) — spot-checked directly against the committed snapshot.
  Tests (a)-(c): three new fixture-driven `generateBuilds` tests (`src/test/generator.test.ts`, "usage-share eligibility floor (T25)" describe) — (a) an item entirely absent from `item_stats` never appears when the eligible pool exactly meets quota (no starvation possible); (b) an item at exactly the floor ratio (100/10000 = 0.01) is picked, one at 99/10000 is excluded, even though both would otherwise be competitive; (c) an early phase stocked with only 2 eligible items backfills its other 2 slots from 3 identically-scored below-floor items in ascending-id tie-break order, proving the fallback pass's stable ordering.
  Test (d) roster sweep found the ticket's own zero-starvation prediction (a single-hero, Kelvin-only estimate) wrong empirically: across all 38 heroes, the fallback fires exactly 5 times — one late-tier item each on Abrams/Kelvin/Mo & Krill/Shiv/Sinclair, always the same item (Siphon Bullets, id 1282141666), a real item with hundreds of recorded matches per hero whose usage *ratio* is diluted by that specific hero's own max-item-match outlier (verified by hand: e.g. Kelvin has 21 of 44 tier-4 items independently above the floor, well over the 5-slot quota, but upgrade-chain exclusivity from earlier picks thins the truly available set down far enough to need the fallback for the last slot) — genuine starvation, not a recurrence of the zero-match bug. Rewrote the test to assert this precisely: every below-floor pick's raw match count is `> 0` (the actual T25 invariant — a zero-match item can never reach a build, fallback or not) and the total fallback count stays bounded (`<= 10`, current real count 5) rather than asserting a literal zero that the data itself disproves.
  Zergggy agreement re-measured via the same real `generateBuilds`/`validateBuildAgainstMatches` path `tune-generator.mjs` uses (temporary harness script, deleted after use, not committed): **48%**, unchanged from T23 — no regression, so no re-tune needed per the ticket's own conditional.
  README gained a new "Usage-share eligibility floor (T25)" subsection (between the T23 synergy section and the high-elo weighting section) covering the root cause, the eligibility-filter design, the two-pass starvation guard, and the roster-sweep finding.
  Verified: `npm run build` clean; `npx vitest run` 78 passed/2 skipped (up from 74 — +4 new T25 tests, 3 fixture + 1 roster sweep); `npm run gate:heldout` OK (6 files in `src/generator/`, clean of held-out references); `npx playwright test` 10/10 (unchanged UI, generator-only ticket).

## CLOSED UNFINISHED — project ended by user 2026-09-01T21:46Z (T26 remained open; all levers implemented, sweep found no improvement over 48%. Moved here verbatim at closure:)

- [ ] **T26 — Infernus build quality: usage must outrank thin-sample win rate; chain stage by usage (user 2026-09-01T20:55Z: "The Infernus build simply looks bad; adjust the numbers to improve it")**
  - Orchestrator diagnosis (measured from the committed snapshots — do not re-derive):
    - Current Infernus build (48% agreement) picks Siphon Bullets (1% Infernus usage share), Unstoppable (5%), Juggernaut (12%), Spirit Rend (23%) — low-usage luxury items riding 52–54% win rates. Item-level WR only spans ~48–54% on Infernus and is inflated by selection bias (luxury items get bought in games already being won), while usage share spans 1–100%: usage is the informative signal, WR is nearly noise without volume behind it.
    - Zergggy staples the build MISSES: Improved Spirit (30/30 of his matches, 98% Infernus usage), Extra Spirit (29/30, 82%), Healbane (30/30, 42%), Escalating Exposure (20/30, 71% usage, 54.0% WR — the single clearest snub), Rapid Recharge (20/30, 58%), Extra Charge (15/30, 47%).
    - Spirit chain: the build shows Boundless Spirit (58% usage) as the chain's pick while Improved Spirit sits at 98% — T18's one-per-chain rule is right, but the STAGE shown should follow usage, not endpoint score alone.
    - T25's starvation fallback re-admits Siphon Bullets on 5 heroes — the fallback should prefer the highest-usage below-floor candidates (stable tie ascending id), so a 1%-usage item is the last resort, not the first fallback.
  - Changes (all behind `ScoreConstants`, the sweep decides the values):
    1. Grid: add usage-dominant weight profiles (usage weight up to ~0.6, win-rate down to ~0.1).
    2. Usage-scaled win-rate confidence: scale the win-rate deviation `(dampedWinRate − meanWinRate)` by `min(1, usageShare / usageConfidenceShare)`; grid `usageConfidenceShare ∈ {0 (off), 0.2, 0.3}` — an item bought in 5% of matches must not ride its WR past mass-usage staples.
    3. Chain-stage selection: when a chain group wins a slot, display/pick the stage with the highest hero usage share (stable tie: ascending item id) instead of the highest-scoring endpoint. T18 one-per-chain and T13 single-build determinism hold.
    4. Fallback ordering fix per the diagnosis above (highest usage first among below-floor).
    5. Include `minUsageShare ∈ {0.01, 0.05, 0.1}` in the grid.
  - Re-run `npm run tune` with the expanded grid (coarse-then-fine, ≤ ~1500 combos), apply the argmax with the usual deterministic ties + all-hero sanity floor.
  - Acceptance: (1) Zergggy agreement > 48%, new number + winning constants in PROGRESS.md; (2) the new Infernus build listed item-by-item in PROGRESS.md with each item's usage%/WR — nothing below the tuned floor except explicit fallbacks; (3) fixture tests for usage-scaled confidence, chain-stage-by-usage, and the fallback ordering; (4) all suites + gates green.
  - Verify: `npm run build` · `npm test` · `npm run gate:heldout` · `npm run test:e2e`.
  - Session update (2026-09-01, see PROGRESS.md for full detail): all 5 changes are implemented (usage-dominant profiles, `usageConfidenceScale`, chain-stage-by-usage display swap behind a new `chainStageByUsage` constant, usage-first starvation fallback, `minUsageShare` grid) and a legitimate 594-combo `npm run tune` sweep ran clean. Acceptance (1) is NOT met: the argmax ties the pre-T26 48% baseline with every new lever at its off/baseline value — none of them improve Zergggy agreement for Infernus on the current snapshot (chain-stage-by-usage measured 48%→34%, see PROGRESS.md for why). Leaving this unchecked rather than force a number. Still open/untried: the diagnosis's fully-missing staples (Healbane, Escalating Exposure, Rapid Recharge, Extra Charge — not chain-mates of anything currently picked, so none of the above 5 changes could have surfaced them) — a next session should check why they never win a slot at all before re-running the sweep.
